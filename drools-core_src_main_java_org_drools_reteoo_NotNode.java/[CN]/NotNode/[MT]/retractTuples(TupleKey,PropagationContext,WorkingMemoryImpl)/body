{
  BetaMemory memory=(BetaMemory)workingMemory.getNodeMemory(this);
  if (memory.remove(leftKey) != null) {
    List keys=new ArrayList(1);
    keys.add(leftKey);
    propagateRetractTuples(keys,context,workingMemory);
  }
}
