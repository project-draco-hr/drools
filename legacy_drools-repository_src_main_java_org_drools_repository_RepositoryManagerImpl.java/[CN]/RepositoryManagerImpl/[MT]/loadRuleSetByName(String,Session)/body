{
  RuleSetDef def=(RuleSetDef)session.createQuery("from RuleSetDef where name = :name").setString("name",ruleSetName).uniqueResult();
  if (def == null) {
    throw new RepositoryException("Unable to find RuleSet with name: [" + ruleSetName + "]");
  }
  return def;
}
