{
  disableHistoryFilter(session);
  String query="from " + asset.getClass().getName() + " where historicalId = :id";
  List result=(List)session.createQuery(query).setLong("id",asset.getId().longValue()).list();
  enableHistoryFilter(session);
  return result;
}
