{
  int start=cursor - 10;
  int end=(cursor + 20);
  if (start < 0) {
    start=0;
  }
  if (end > expr.length) {
    end=expr.length - 1;
  }
  return "'" + copyValueOf(expr,start,end - start) + "'";
}
