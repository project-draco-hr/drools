{
  this.rete=new Rete(this);
  this.reteooBuilder=new ReteooBuilder(this);
  EntryPointNode epn=new EntryPointNode(this.reteooBuilder.getIdGenerator().getNextId(),RuleBasePartitionId.MAIN_PARTITION,this.getConfig().isMultithreadEvaluation(),this.rete,EntryPoint.DEFAULT);
  epn.attach();
}
