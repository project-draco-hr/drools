{
  if (this.out) {
    session.getBatchExecutionResult().getResults().put((this.outIdentifier != null) ? this.outIdentifier : this.identifier,object);
  }
  session.setGlobal(this.identifier,this.object);
  return null;
}
