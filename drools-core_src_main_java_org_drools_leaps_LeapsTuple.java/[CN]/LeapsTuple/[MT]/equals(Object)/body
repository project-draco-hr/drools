{
  if (this == that)   return true;
  if (!(that instanceof LeapsTuple))   return false;
  for (int i=0; i < this.factHandles.length; i++) {
    if (!this.factHandles[i].equals(((LeapsTuple)that).getFactHandleAtPosition(i))) {
      return false;
    }
  }
  return true;
}
