{
  if (set.isEmpty()) {
    if (set.isEmpty()) {
      this.tms.getJustifiedMap().remove(handle.getId());
      workingMemory.retract(this.handle,false,true,context.getRuleOrigin(),context.getActivationOrigin());
    }
  }
}
