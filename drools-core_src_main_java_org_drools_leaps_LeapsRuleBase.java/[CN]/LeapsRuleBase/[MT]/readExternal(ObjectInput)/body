{
  doReadExternal(stream,new Object[0]);
  for (int i=0; i < this.getPackages().length; i++) {
    final Rule[] rules=this.getPackages()[i].getRules();
    for (int j=0; j < rules.length; ++j) {
      addRule(rules[j]);
    }
  }
}
