{
  KnowledgeBaseEventListenerWrapper wrapper=new KnowledgeBaseEventListenerWrapper(this,listener);
  this.mappedKnowledgeBaseListeners.put(listener,wrapper);
  this.ruleBase.addEventListener(wrapper);
}
