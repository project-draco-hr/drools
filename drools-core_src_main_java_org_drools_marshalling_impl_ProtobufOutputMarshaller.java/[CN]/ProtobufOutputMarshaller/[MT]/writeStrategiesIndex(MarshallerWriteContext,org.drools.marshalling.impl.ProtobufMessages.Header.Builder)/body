{
  for (  Entry<String,Integer> entry : context.usedStrategies.entrySet()) {
    _header.addStrategy(ProtobufMessages.Header.StrategyIndex.newBuilder().setId(entry.getValue().intValue()).setName(entry.getKey()).build());
  }
}
