{
  String drl="rule \"rule1\"\n" + "calendars \"myCalendar\"\n" + "when\n"+ "then\n"+ "end";
  RuleModel m=BRDRLPersistence.getInstance().unmarshal(drl,dmo);
  assertEquals(1,m.attributes.length);
  assertEquals("calendars",m.attributes[0].getAttributeName());
  assertEquals("myCalendar",m.attributes[0].getValue());
}
