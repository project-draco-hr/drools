{
  final List joined=tupleMatch.getJoinedTuples();
  for (int i=0, size=joined.size(); i < size; i++) {
    ((TupleSink)getTupleSinks().get(i)).retractTuple((ReteTuple)joined.get(i),context,workingMemory);
  }
}
