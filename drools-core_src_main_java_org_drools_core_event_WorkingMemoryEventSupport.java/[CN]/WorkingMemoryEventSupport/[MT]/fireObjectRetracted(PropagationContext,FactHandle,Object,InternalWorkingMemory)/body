{
  final Iterator<WorkingMemoryEventListener> iter=getEventListenersIterator();
  if (iter.hasNext()) {
    final ObjectRetractedEvent event=new ObjectRetractedEvent(workingMemory,propagationContext,handle,oldObject);
    do {
      iter.next().objectRetracted(event);
    }
 while (iter.hasNext());
  }
}
