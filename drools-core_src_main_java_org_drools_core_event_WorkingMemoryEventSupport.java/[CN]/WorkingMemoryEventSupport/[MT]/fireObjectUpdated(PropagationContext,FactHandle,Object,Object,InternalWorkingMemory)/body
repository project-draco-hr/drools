{
  final Iterator<WorkingMemoryEventListener> iter=getEventListenersIterator();
  if (iter.hasNext()) {
    final ObjectUpdatedEvent event=new ObjectUpdatedEvent(workingMemory,propagationContext,handle,oldObject,object);
    do {
      iter.next().objectUpdated(event);
    }
 while (iter.hasNext());
  }
}
