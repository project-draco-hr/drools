{
  filterLogEvent(new RuleFlowLogEvent(LogEvent.AFTER_RULEFLOW_COMPLETED,event.getProcessInstance().getProcess().getId(),event.getProcessInstance().getProcess().getName(),event.getProcessInstance().getId()));
}
