{
  filterLogEvent(new RuleFlowLogEvent(LogEvent.BEFORE_RULEFLOW_CREATED,event.getProcessInstance().getProcess().getId(),event.getProcessInstance().getProcess().getName(),event.getProcessInstance().getId()));
}
