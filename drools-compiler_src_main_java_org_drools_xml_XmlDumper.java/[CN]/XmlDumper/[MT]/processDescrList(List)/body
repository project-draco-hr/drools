{
  String descrString="";
  for (Iterator iterator=descr.iterator(); iterator.hasNext(); ) {
    visit(iterator.next());
    descrString+=template;
    descrString+=eol;
  }
  return descrString + eol;
}
