{
  int index=-1;
  for (int i=0; i < array.length; i++) {
    if ((array[i] == null && value == null) || (array[i] != null && array[i].equals(value))) {
      index=i;
      break;
    }
  }
  return index;
}
