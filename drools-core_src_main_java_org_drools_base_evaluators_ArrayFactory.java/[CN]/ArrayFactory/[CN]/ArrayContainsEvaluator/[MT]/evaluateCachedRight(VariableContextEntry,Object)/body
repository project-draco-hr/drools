{
  final Object value=context.declaration.getExtractor().getValue(left);
  final Object[] array=(Object[])((ObjectVariableContextEntry)context).right;
  if (Arrays.binarySearch(array,value) == -1) {
    return false;
  }
  return true;
}
