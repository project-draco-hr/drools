{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Bird bird=(Bird)o;
  if (!name.equals(bird.name)) {
    return false;
  }
  if (!species.equals(bird.species)) {
    return false;
  }
  return true;
}
