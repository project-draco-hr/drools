{
  final int Synpred1_fragment_StartIndex=this.input.index();
  try {
    if (this.backtracking > 0 && alreadyParsedRule(this.input,60)) {
      return;
    }
{
      match("\r\n");
      if (this.failed) {
        return;
      }
    }
  }
  finally {
    if (this.backtracking > 0) {
      memoize(this.input,60,Synpred1_fragment_StartIndex);
    }
  }
}
