{
  if (!tableFinished && rowNumber >= startRow) {
    if (currentRow != null && currentRow.isEmpty()) {
      currentRow=null;
      tableFinished=true;
    }
 else {
      if (currentRow != null)       session.assertObject(currentRow);
      currentRow=new Row(rowNumber,columns);
    }
  }
}
