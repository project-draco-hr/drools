{
  if (!tableFinished && rowNumber >= startRow) {
    if (currentRow != null && currentRow.cells.isEmpty()) {
      currentRow=null;
      tableFinished=true;
    }
 else {
      if (currentRow != null)       wm.assertObject(currentRow);
      currentRow=new Row(rowNumber);
    }
  }
}
