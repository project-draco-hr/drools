{
  try {
    KnowledgeBuilder kbuilder=KnowledgeBuilderFactory.newKnowledgeBuilder();
    kbuilder.add(ResourceFactory.newClassPathResource("logging.drl",OrderExample.class),KnowledgeType.DRL);
    kbase.addKnowledgePackages(kbuilder.getKnowledgePackages());
  }
 catch (  Throwable t) {
    t.printStackTrace();
  }
}
