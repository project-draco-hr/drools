{
  short token=-1;
  while ((token=inCtx.readShort()) != PersisterEnums.END) {
    int i=inCtx.readInt();
    Object object=((Object[])betaMemory.getBehaviorContext())[i];
switch (token) {
case PersisterEnums.SLIDING_TIME_WIN:
{
        readSlidingTimeWindowBehaviour(betaNode,betaMemory,(SlidingTimeWindow)betaNode.getBehaviors()[i],(SlidingTimeWindowContext)object,inCtx);
        break;
      }
case PersisterEnums.SLIDING_LENGTH_WIN:
{
      readSlidingLengthWindowBehaviour(betaNode,betaMemory,(SlidingLengthWindow)betaNode.getBehaviors()[i],(SlidingLengthWindowContext)object,inCtx);
      break;
    }
}
}
}
