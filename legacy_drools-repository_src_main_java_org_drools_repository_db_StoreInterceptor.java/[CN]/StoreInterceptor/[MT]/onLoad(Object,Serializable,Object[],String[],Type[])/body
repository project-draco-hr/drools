{
  checkACLAllowed(entity,id,"You are not authorized to load this asset.",AssetPermission.READ);
  checkACLDenied(entity,id,"You have been blocked from loading this asset.",AssetPermission.DENY_READ);
  return false;
}
