{
  try {
    PropertyInfo propertyInfo=(PropertyInfo)obj;
    if (var != null && propertyInfo.getVar() != null && var.equals(propertyInfo.getVar())) {
      return true;
    }
    if (name != null && clazz != null && propertyInfo.getName() != null && propertyInfo.getClazz() != null && name.equals(propertyInfo.getName()) && clazz.equals(propertyInfo.getClazz())) {
      return true;
    }
  }
  finally {
  }
  return false;
}
