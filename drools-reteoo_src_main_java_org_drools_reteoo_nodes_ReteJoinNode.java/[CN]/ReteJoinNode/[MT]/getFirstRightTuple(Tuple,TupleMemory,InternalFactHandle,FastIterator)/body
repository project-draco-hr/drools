{
  if (!this.indexedUnificationJoin) {
    return memory instanceof RightTupleIndexHashTable ? (RightTuple)((RightTupleIndexHashTable)memory).getFirst(leftTuple,factHandle) : (RightTuple)memory.getFirst(leftTuple);
  }
 else {
    return (RightTuple)it.next(null);
  }
}
