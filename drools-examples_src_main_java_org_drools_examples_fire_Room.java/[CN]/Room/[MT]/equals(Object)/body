{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Room room=(Room)o;
  if (!name.equals(room.name)) {
    return false;
  }
  return true;
}
