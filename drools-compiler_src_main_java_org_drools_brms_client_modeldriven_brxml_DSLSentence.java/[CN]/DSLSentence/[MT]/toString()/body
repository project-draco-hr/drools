{
  char[] chars=sentence.toCharArray();
  String result="";
  for (int i=0; i < chars.length; i++) {
    char c=chars[i];
    if (c != '{' && c != '}') {
      result+=c;
    }
  }
  return result;
}
