{
  Class clazz=null;
  try {
    clazz=this.typeResolver.resolveType(type);
  }
 catch (  ClassNotFoundException e) {
    throw new RuntimeException(e);
  }
  return new LocalVariable(identifier,clazz,isFinal);
}
