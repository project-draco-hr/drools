{
  builder.append(ASSERT_METHOD_SIGNATURE).append(NEWLINE);
  if (alphaNetContainsHashedField) {
    builder.append(factClassName).append(" ").append(LOCAL_FACT_VAR_NAME).append(" = (").append(factClassName).append(")").append(FACT_HANDLE_PARAM_NAME).append(".getObject();").append(NEWLINE);
  }
}
