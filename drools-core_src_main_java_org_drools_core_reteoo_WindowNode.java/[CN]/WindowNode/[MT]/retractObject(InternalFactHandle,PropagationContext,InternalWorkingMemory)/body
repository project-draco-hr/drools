{
  final WindowMemory memory=(WindowMemory)workingMemory.getNodeMemory(this);
  memory.gate.lock();
  try {
    behavior.retractFact(memory,factHandle,workingMemory);
    memory.events.remove(factHandle);
  }
  finally {
    memory.gate.unlock();
  }
}
