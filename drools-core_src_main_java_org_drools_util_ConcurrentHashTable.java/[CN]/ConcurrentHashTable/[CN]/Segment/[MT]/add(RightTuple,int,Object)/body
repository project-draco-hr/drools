{
  lock();
  try {
    final RightTupleList entry=getOrCreate(hashCode,object);
    rightTuple.setMemory(entry);
    entry.add(rightTuple);
    this.tupleCount++;
  }
  finally {
    unlock();
  }
}
