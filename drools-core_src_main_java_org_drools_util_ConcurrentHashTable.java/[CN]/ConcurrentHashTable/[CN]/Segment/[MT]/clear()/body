{
  if (tupleCount != 0) {
    lock();
    try {
      RightTupleList[] tab=table;
      for (int i=0; i < tab.length; i++)       tab[i]=null;
      ++modCount;
      tupleCount=0;
      keyCount=0;
    }
  finally {
      unlock();
    }
  }
}
