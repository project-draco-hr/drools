{
  return !(d instanceof ConstraintConnectiveDescr) || ((ConstraintConnectiveDescr)d).getConnective() != ConnectiveType.OR ? expr : rewriteCompositeExpressions(context,pattern,(ConstraintConnectiveDescr)d);
}
