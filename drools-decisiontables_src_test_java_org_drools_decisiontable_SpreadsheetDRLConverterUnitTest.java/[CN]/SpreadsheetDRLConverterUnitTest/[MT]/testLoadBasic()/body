{
  SpreadsheetDRLConverter converter=new SpreadsheetDRLConverter();
  InputStream stream=this.getClass().getResourceAsStream("/data/BasicWorkbook.xls");
  String drl=converter.convertToDRL(stream,InputType.XLS);
  assertNotNull(drl);
}
