{
  column.setOffset(context.getCurrentColumnOffset());
  context.incrementCurrentColumnOffset();
  final List predicates=attachAlphaNodes(context,utils,column);
  final BetaConstraints binder=utils.createBetaNodeConstraint(context,predicates);
  return binder;
}
