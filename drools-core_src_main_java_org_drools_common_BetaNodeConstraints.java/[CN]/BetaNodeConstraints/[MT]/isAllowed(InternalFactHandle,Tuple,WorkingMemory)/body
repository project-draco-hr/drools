{
  if (this.constraints == null) {
    return true;
  }
  for (int i=0; i < this.constraints.length; i++) {
    if (!this.constraints[i].isAllowed(handle.getObject(),tuple,workingMemory)) {
      return false;
    }
  }
  return true;
}
