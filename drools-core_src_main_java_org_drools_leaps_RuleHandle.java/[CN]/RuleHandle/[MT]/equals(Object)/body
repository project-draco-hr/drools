{
  if (this == that)   return true;
  if (!(that instanceof RuleHandle))   return false;
  return (this.getId() == ((RuleHandle)that).getId() && this.getLeapsRule().equals(((RuleHandle)that).getLeapsRule()) && (this.getDominantPosition() == ((RuleHandle)that).getDominantPosition()));
}
