{
  String temp=this.varNameMap.get(name);
  if (temp == null) {
    temp=makeValid(name);
    if (!temp.equals(name)) {
      this.varNameMap.put(name,temp);
    }
  }
  this.vars.put(temp,value);
}
