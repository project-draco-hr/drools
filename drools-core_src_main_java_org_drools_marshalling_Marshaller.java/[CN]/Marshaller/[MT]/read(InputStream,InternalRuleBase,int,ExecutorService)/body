{
  WMSerialisationInContext context=new WMSerialisationInContext(stream,ruleBase,RuleBaseNodes.getNodeMap(ruleBase),factory);
  ReteooStatefulSession session=InputPersister.readSession(context,id,executor);
  context.close();
  return session;
}
