{
  WMSerialisationOutContext context=new WMSerialisationOutContext(stream,ruleBase,(InternalWorkingMemory)session,RuleBaseNodes.getNodeMap(ruleBase),this.factory);
  OutputPersister.writeSession(context);
  context.close();
}
