{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  EnterEvent enterEvent=(EnterEvent)o;
  if (!character.equals(enterEvent.character)) {
    return false;
  }
  if (!room.equals(enterEvent.room)) {
    return false;
  }
  return true;
}
