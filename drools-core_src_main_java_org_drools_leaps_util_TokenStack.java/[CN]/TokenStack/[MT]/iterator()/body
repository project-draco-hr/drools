{
  return new Iterator(){
    Iterator it=TokenStack.this.map.values().iterator();
    public boolean hasNext(){
      return this.it.hasNext();
    }
    public void remove(){
    }
    public Object next(){
      return this.it.next();
    }
  }
;
}
