{
  return new Iterator(){
    Iterator it=map.values().iterator();
    public boolean hasNext(){
      return it.hasNext();
    }
    public void remove(){
    }
    public Object next(){
      return it.next();
    }
  }
;
}
