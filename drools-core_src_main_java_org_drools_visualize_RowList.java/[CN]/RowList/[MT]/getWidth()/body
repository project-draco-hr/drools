{
  int width=0;
  for (Iterator rowIter=rows.iterator(); rowIter.hasNext(); ) {
    Row row=(Row)rowIter.next();
    int rowWidth=row.getWidth();
    if (rowWidth > width) {
      width=rowWidth;
    }
  }
  return width;
}
