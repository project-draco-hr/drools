{
  for (int i=0; i < attributes.length; i++) {
    final RuleAttribute at=attributes[i];
    final AttributeDescr attr=new AttributeDescr(at.attributeName,at.value == null ? "true" : at.value);
    rule.addAttribute(attr);
  }
}
