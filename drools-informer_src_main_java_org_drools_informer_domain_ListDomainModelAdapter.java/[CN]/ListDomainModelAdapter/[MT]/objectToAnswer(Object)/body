{
  StringBuilder b=new StringBuilder();
  List<String> l=(List<String>)object;
  String delimiter="";
  for (  String s : l) {
    b.append(delimiter);
    b.append(s);
    delimiter=",";
  }
  return b.toString();
}
