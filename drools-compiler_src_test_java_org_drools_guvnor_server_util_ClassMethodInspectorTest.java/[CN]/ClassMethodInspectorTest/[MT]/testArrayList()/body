{
  final ClassMethodInspector ext=new ClassMethodInspector(ArrayList.class);
  for (  String s : ext.getMethodNames()) {
    assertFalse("Method " + s + " is not allowed.",allowedListMethod(s));
  }
}
