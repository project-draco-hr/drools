{
  if (!node.isInUse()) {
    removeObjectSink((ObjectSink)node);
  }
  removeShare(builder);
  if (!this.isInUse()) {
    for (int i=0, length=workingMemories.length; i < length; i++) {
      workingMemories[i].clearNodeMemory(this);
    }
  }
  this.objectSource.remove(builder,this,workingMemories);
}
