{
  RightTuple rightTuple=null;
  if (context.getActiveWindowTupleList() == null) {
    rightTuple=new RightTuple(handle,sink);
  }
 else {
    rightTuple=new WindowTuple(handle,sink,context.getActiveWindowTupleList());
  }
  rightTuple.setPropagationContext(context);
  return rightTuple;
}
