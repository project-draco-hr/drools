{
  PathMemory pmem=isStreamMode() ? smem.getPathMemories().get(0) : smem.getFirstDataDrivenPathMemory();
  if (pmem != null) {
    forceFlushLeftTuple(pmem,smem,wm,null);
    return true;
  }
  return false;
}
