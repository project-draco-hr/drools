{
  if (isEvent) {
    SessionClock clock=((TemporalSession)workingMemory).getSessionClock();
    return new EventFactHandle(id,object,recency,clock.getCurrentTime(),0);
  }
 else {
    return new DefaultFactHandle(id,object,recency);
  }
}
