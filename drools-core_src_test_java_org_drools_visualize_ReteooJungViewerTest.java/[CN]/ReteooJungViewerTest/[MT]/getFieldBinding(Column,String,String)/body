{
  Class clazz=((ClassObjectType)column.getObjectType()).getClassType();
  int index=getIndex(clazz,fieldName);
  FieldExtractor extractor=new ClassFieldExtractor(clazz,index);
  return new FieldBinding(declarationName,null,extractor,column.getIndex());
}
