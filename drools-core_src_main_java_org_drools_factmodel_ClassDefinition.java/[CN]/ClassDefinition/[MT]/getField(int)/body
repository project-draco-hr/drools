{
  if (index >= fields.size() || index < 0)   throw new IndexOutOfBoundsException("Error trying to access field at position " + index);
  Iterator<FieldDefinition> iter=fields.values().iterator();
  for (int j=0; j < index; j++)   iter.next();
  return iter.next();
}
