{
  Rule rule=context.getRule();
  for (  String metaAttr : context.getRuleDescr().getAnnotationNames()) {
    rule.addMetaAttribute(metaAttr,context.getRuleDescr().getAnnotation(metaAttr).getValue());
  }
}
