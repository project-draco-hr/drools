{
  int pos=cursor;
  for (pos--; pos > 0; pos--) {
    if (property[pos] == '\'' || property[pos] == '"')     return pos;
 else     if (!isWhitespace(property[pos]))     return pos;
  }
  return -1;
}
