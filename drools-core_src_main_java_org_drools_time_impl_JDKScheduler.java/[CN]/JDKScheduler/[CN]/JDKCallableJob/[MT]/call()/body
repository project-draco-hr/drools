{
  this.job.execute(this.ctx);
  Date date=this.trigger.getNextFireTime();
  if (date != null) {
    ScheduledFuture<Void> future=schedule(date,this,this.scheduler);
    this.handle.setFuture(future);
  }
  return null;
}
