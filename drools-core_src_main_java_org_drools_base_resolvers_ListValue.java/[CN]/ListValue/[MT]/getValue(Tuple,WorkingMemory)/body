{
  final List resolvedList=new ArrayList(this.list.size());
  for (final Iterator it=this.list.iterator(); it.hasNext(); ) {
    resolvedList.add(((ValueHandler)it.next()).getValue(tuple,wm));
  }
  return resolvedList;
}
