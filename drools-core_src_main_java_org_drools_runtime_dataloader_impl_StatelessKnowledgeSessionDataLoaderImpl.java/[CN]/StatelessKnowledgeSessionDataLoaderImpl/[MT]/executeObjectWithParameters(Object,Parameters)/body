{
  StatelessKnowledgeSessionPipelineContext context=new StatelessKnowledgeSessionPipelineContext(this.ksession);
  this.pipeline.receive(object,context);
  return this.ksession.executeObjectWithParameters(context.getResult().get(0),parameters);
}
