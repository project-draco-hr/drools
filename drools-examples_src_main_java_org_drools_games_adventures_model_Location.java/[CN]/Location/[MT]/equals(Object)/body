{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Location location=(Location)o;
  if (!target.equals(location.target)) {
    return false;
  }
  if (!thing.equals(location.thing)) {
    return false;
  }
  return true;
}
