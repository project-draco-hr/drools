{
  if (tag == Tag.I) {
    listener.startComment();
  }
 else   if (tag == Tag.TABLE) {
    listener.startTable();
  }
 else   if (tag == Tag.TH) {
    listener.startRow();
  }
 else   if (tag == Tag.TR) {
    listener.startRow();
  }
 else   if (tag == Tag.TD) {
    listener.startColumn();
  }
}
