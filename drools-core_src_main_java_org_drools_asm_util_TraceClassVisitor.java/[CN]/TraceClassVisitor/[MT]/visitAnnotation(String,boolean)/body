{
  this.text.add("\n");
  final AnnotationVisitor tav=super.visitAnnotation(desc,visible);
  if (this.cv != null) {
    ((TraceAnnotationVisitor)tav).av=this.cv.visitAnnotation(desc,visible);
  }
  return tav;
}
