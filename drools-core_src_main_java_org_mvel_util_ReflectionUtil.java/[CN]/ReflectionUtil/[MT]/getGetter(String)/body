{
  char[] c=s.toCharArray();
  char[] chars=new char[c.length + 3];
  chars[0]='g';
  chars[1]='e';
  chars[2]='t';
  if (s.charAt(0) > 'Z')   chars[3]=(char)(c[0] - ('z' - 'Z'));
  System.arraycopy(c,1,chars,4,c.length - 1);
  return new String(chars);
}
