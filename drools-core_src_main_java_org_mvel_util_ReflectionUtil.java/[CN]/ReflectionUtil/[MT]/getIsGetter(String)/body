{
  char[] c=s.toCharArray();
  char[] chars=new char[c.length + 2];
  chars[0]='i';
  chars[1]='s';
  if (s.charAt(0) > 'Z')   chars[2]=(char)(s.charAt(0) - ('z' - 'Z'));
  System.arraycopy(c,1,chars,3,c.length - 1);
  return new String(chars);
}
