{
  WorkingMemoryImpl workingMemory=new WorkingMemoryImpl(new RuleBaseImpl());
  Rule rule=new Rule("test-rule");
  PropagationContext context=new PropagationContextImpl(0,PropagationContext.ASSERTION,null,null);
  MockTupleSource source=new MockTupleSource(1);
  TestNode node=new TestNode(1,source,new MockCondition(null,true),false);
  node.attach();
  assertEquals(0,source.getUdated());
  node.updateNewNode(workingMemory,null);
  assertEquals(1,source.getUdated());
}
