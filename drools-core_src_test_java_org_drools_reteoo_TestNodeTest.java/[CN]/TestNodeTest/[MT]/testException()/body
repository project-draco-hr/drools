{
  MockCondition condition=new MockCondition(null,true);
  condition.setTestException(true);
  TestNode node=new TestNode(1,new MockTupleSource(15),condition);
  MockTupleSink sink=new MockTupleSink();
  node.addTupleSink(sink);
  FactHandleImpl f0=new FactHandleImpl(0);
  ReteTuple tuple=new ReteTuple(0,f0,workingMemory);
  try {
    node.assertTuple(tuple,context,this.workingMemory);
    fail("Should have thrown TestException");
  }
 catch (  TestException e) {
  }
}
