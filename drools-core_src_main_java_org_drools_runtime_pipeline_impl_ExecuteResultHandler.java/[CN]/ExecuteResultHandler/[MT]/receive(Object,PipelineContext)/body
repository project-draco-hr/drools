{
  context.getResultHandler().handleResult(context.getResult());
  try {
    context.getResultHandler().handleResult(context.getResult());
  }
 catch (  Exception e) {
    handleException(this,object,e);
  }
  emit(object,context);
}
