{
  RightOnlyNode node=new RightOnlyNode("something here","<<${right}.somethingHere()",1);
  node.getArguments().add(new SubstitutionNode("arg 1","arg1"));
  assertEquals("arg1.somethingHere()",node.render());
}
