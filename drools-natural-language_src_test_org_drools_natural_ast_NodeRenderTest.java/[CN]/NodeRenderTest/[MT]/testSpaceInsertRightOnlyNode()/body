{
  LiteralNode lit1=new LiteralNode("bbb");
  RightOnlyNode right=new RightOnlyNode("blah","blah(${1})",1);
  LiteralNode lit2=new LiteralNode("ccc");
  lit1.next=right;
  right.prev=lit1;
  right.next=lit2;
  lit2.prev=right;
  lit1.processLeftToRight(RightOnlyNode.class);
  assertEquals(" blah(ccc)",right.render());
  right.prev=null;
  assertEquals("blah(ccc)",right.render());
}
