{
  boolean ret=false;
  if ((matches != null) && (matches.getChild() != null) && (matches.getChild().getLinkedList() != null)) {
    ObjectMatches om=(ObjectMatches)matches.getNode();
    ret=((matches.getChild().getLinkedList() != noMatchList) || (!isMatch(workingMemory,om.getFactHandle())));
    if (ret && (this.childMemory != null)) {
      ret=this.childMemory.isPossibleMatch((MultiLinkedListNodeWrapper)matches.getChild());
    }
  }
  return ret;
}
