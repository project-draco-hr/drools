{
  if (isEvent) {
    SessionClock clock=((TemporalSession)workingMemory).getSessionClock();
    return new Jsr94EventFactHandle(id,object,recency,clock.getCurrentTime(),duration);
  }
 else {
    return new Jsr94FactHandle(id,object,recency);
  }
}
