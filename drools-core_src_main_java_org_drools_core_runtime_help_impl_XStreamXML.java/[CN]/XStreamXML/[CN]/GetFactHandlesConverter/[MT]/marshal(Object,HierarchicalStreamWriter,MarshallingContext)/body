{
  GetFactHandlesCommand cmd=(GetFactHandlesCommand)object;
  writer.addAttribute("disconnected","" + cmd.isDisconnected());
  if (cmd.getOutIdentifier() != null) {
    writer.addAttribute("out-identifier",cmd.getOutIdentifier());
  }
}
