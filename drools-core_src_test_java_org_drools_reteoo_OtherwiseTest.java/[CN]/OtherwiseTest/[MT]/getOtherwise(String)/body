{
  final Rule rule=new Rule(name);
  final Column pat=new Column(0,new ClassObjectType(Otherwise.class,ShadowProxyFactory.getProxy(Otherwise.class)));
  rule.addPattern(pat);
  rule.setConsequence(new MockConsequence());
  return rule;
}
