{
  final Rule rule=new Rule(name);
  final Column pat=new Column(0,new ClassObjectType(TestBean.class,ShadowProxyFactory.getProxy(TestBean.class)));
  rule.addPattern(pat);
  rule.setConsequence(new MockConsequence());
  return rule;
}
