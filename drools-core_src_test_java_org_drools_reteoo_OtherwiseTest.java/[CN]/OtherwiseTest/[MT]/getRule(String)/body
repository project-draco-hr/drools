{
  final Rule rule=new Rule(name);
  final Pattern pat=new Pattern(0,new ClassObjectType(TestBean.class,ShadowProxyFactory.getProxy(TestBean.class)));
  rule.addPattern(pat);
  rule.setConsequence(new MockConsequence());
  return rule;
}
