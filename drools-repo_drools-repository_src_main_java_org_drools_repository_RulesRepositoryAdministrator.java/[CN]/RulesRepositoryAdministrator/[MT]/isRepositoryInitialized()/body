{
  try {
    return isNamespaceRegistered(session) && session.getRootNode().hasNode(RulesRepository.RULES_REPOSITORY_NAME);
  }
 catch (  RepositoryException e) {
    throw new RulesRepositoryException(e);
  }
}
