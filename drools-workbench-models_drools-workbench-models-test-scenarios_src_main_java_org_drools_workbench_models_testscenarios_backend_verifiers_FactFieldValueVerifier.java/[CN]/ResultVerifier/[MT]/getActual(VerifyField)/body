{
  Object actualValue=MVELSafeHelper.getEvaluator().executeExpression(new ExpressionCompiler("__fact__." + currentField.getFieldName(),parserContext).compile(),variables);
  return (actualValue != null) ? actualValue.toString() : "";
}
