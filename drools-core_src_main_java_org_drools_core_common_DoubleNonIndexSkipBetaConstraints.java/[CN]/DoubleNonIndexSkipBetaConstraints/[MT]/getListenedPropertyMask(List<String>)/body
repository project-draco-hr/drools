{
  if (constraint0 instanceof MvelConstraint && constraint1 instanceof MvelConstraint) {
    return ((MvelConstraint)constraint0).getListenedPropertyMask(settableProperties) | ((MvelConstraint)constraint1).getListenedPropertyMask(settableProperties);
  }
  return Long.MAX_VALUE;
}
