{
  return new DefaultFactHandle(originalHandle.getId(),(originalHandle.getEntryPoint() != null) ? originalHandle.getEntryPoint().getEntryPointId() : null,originalHandle.getIdentityHashCode(),originalHandle.getObjectHashCode(),originalHandle.getRecency(),originalHandle.getObject());
}
