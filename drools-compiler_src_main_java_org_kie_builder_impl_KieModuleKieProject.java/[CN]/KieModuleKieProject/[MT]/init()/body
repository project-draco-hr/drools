{
  if (kieModules == null) {
    kieModules=new HashMap<GAV,InternalKieModule>();
    kieModules.putAll(kieModule.getDependencies());
    kieModules.put(kieModule.getGAV(),kieModule);
    indexParts(kieModules,kJarFromKBaseName);
    initClassLaoder();
  }
}
