{
  if ((fields & NUMERIC) != 0) {
    value=numericValue=convert(compiledAccessor.getValue(ctx,elCtx,variableFactory),BigDecimal.class);
  }
 else   value=compiledAccessor.getValue(ctx,elCtx,variableFactory);
  if ((fields & NEGATION) != 0)   value=!((Boolean)value);
  return this;
}
