{
  if ((vars.contains(ArgumentNode.LEFT) || vars.contains(ArgumentNode.RIGHT)) && (vars.contains("-1") || vars.contains("1"))) {
    throw new NaturalLanguageException("The following expression: [" + token + "] mixes ${left} and ${1}"+ " style notation. Please stick to one type.");
  }
}
