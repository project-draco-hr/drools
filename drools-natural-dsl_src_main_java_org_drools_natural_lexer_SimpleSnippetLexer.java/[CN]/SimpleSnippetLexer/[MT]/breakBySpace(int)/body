{
  if (inDoubleQuotes) {
    appendCurrentChar(pos);
  }
 else   if (bracketStack.size() > 0) {
    if (previousIsNotSpace(pos)) {
      appendCurrentChar(pos);
    }
  }
 else {
    newToken();
  }
}
