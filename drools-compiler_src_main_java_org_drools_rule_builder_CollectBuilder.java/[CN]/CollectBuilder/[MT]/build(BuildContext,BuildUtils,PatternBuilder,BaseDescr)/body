{
  final CollectDescr collectDescr=(CollectDescr)descr;
  final Pattern sourcePattern=patternBuilder.build(context,utils,collectDescr.getSourcePattern());
  if (sourcePattern == null) {
    return null;
  }
  final Pattern resultPattern=patternBuilder.build(context,utils,collectDescr.getResultPattern());
  final String className="collect" + context.getNextId();
  collectDescr.setClassMethodName(className);
  final Collect collect=new Collect(sourcePattern,resultPattern);
  return collect;
}
