{
  final CollectDescr collectDescr=(CollectDescr)descr;
  final Column sourceColumn=columnBuilder.build(context,utils,collectDescr.getSourceColumn());
  if (sourceColumn == null) {
    return null;
  }
  final Column resultColumn=columnBuilder.build(context,utils,collectDescr.getResultColumn());
  final String className="collect" + context.getNextId();
  collectDescr.setClassMethodName(className);
  final Collect collect=new Collect(sourceColumn,resultColumn);
  return collect;
}
