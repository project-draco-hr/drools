{
  if (obj instanceof Entry)   return obj.hashCode();
  if (obj instanceof InternalFactHandle)   return rehash(((InternalFactHandle)obj).getIdentityHashCode());
  return rehash(System.identityHashCode(obj));
}
