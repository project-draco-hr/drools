{
  for (  String fileName : srcMfs.getFileNames()) {
    if (filterFileInKBase(kieBase,fileName)) {
      ckbuilder.add(ResourceFactory.newByteArrayResource(srcMfs.getBytes(fileName)),ResourceType.determineResourceType(fileName));
    }
  }
}
