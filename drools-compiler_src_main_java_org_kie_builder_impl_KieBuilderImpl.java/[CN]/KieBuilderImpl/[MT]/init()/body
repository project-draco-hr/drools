{
  KieServices ks=KieServices.Factory.get();
  results=new ResultsImpl();
  buildPomModel();
  buildKieModuleModel();
  if (pomModel != null) {
    gav=ks.newGav(pomModel.getGroupId(),pomModel.getArtifactId(),pomModel.getVersion());
  }
}
