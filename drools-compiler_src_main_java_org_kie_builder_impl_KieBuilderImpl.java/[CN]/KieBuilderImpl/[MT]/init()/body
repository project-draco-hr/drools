{
  KieFactory kf=KieFactory.Factory.get();
  messages=new Messages();
  buildPomModel();
  buildKieModuleModel();
  if (pomModel != null) {
    gav=kf.newGav(pomModel.getGroupId(),pomModel.getArtifactId(),pomModel.getVersion());
  }
}
