{
  KieServices ks=KieServices.Factory.get();
  results=new ResultsImpl();
  buildPomModel();
  buildKieModuleModel();
  if (pomModel != null) {
    releaseId=ks.newReleaseId(pomModel.getGroupId(),pomModel.getArtifactId(),pomModel.getVersion());
  }
}
