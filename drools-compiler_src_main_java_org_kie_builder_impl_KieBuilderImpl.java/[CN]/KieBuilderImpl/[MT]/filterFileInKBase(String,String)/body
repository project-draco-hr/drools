{
  return (fileName.startsWith(kBaseName + "/") || fileName.contains("/" + kBaseName + "/")) && (fileName.endsWith(ResourceType.DRL.getDefaultExtension()) || fileName.endsWith(ResourceType.BPMN2.getDefaultExtension()));
}
