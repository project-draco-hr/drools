{
  String pathName=kBaseName.replace('.','/');
  return (fileName.startsWith("src/main/resoureces/" + pathName + "/") || fileName.contains("/" + pathName + "/")) && (fileName.endsWith(ResourceType.DRL.getDefaultExtension()) || fileName.endsWith(ResourceType.BPMN2.getDefaultExtension()));
}
