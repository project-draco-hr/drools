{
  if (!isBuilt()) {
    kieJar=new MemoryKieJar(kieProject,mfs);
    compileJavaClasses();
    compileKieFiles();
    KieServices.Factory.get().getKieRepository().addKieJar(kieJar);
  }
  return messages;
}
