{
  if (!isBuilt()) {
    trgMfs=new MemoryFileSystem();
    kieJar=new MemoryKieJar(kieProject,trgMfs);
    writePomAndKProject();
    compileJavaClasses();
    compileKieFiles();
    if (!hasMessages(Level.ERROR)) {
      KieServices.Factory.get().getKieRepository().addKieJar(kieJar);
    }
  }
  return new MessagesImpl(messages,null);
}
