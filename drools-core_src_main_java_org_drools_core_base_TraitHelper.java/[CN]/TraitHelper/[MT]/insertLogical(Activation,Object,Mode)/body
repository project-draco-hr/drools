{
  if (!activation.isMatched()) {
    return;
  }
  FactHandle handle=workingMemory.getTruthMaintenanceSystem().insert(object,modes,activation.getRule(),activation);
  if (this.identityMap != null) {
    this.identityMap.put(object,handle);
  }
}
