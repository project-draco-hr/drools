{
  if (!activation.isMatched()) {
    return;
  }
  FactHandle handle=this.workingMemory.insert(object,modes,false,true,activation.getRule(),activation);
  if (this.identityMap != null) {
    this.identityMap.put(object,handle);
  }
}
