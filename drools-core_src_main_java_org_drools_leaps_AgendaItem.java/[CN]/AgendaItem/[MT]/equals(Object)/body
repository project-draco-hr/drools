{
  if (object == this) {
    return true;
  }
  if ((object == null) || !(object instanceof AgendaItem)) {
    return false;
  }
  AgendaItem otherItem=(AgendaItem)object;
  return (this.rule.equals(otherItem.getRule()) && this.tuple.getDominantFactHandle().equals(((Token)otherItem.getTuple()).getDominantFactHandle()));
}
