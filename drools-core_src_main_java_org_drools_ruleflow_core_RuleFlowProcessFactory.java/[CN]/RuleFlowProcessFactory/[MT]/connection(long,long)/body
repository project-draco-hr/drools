{
  Node from=process.getNode(fromId);
  Node to=process.getNode(toId);
  new ConnectionImpl(from,org.drools.workflow.core.Node.CONNECTION_DEFAULT_TYPE,to,org.drools.workflow.core.Node.CONNECTION_DEFAULT_TYPE);
  return this;
}
