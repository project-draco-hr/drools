{
  final Object value=this.expression.evaluate(handle.getObject(),leftTuple,declarations,innerDeclarations,workingMemory,workingMemoryContext).getValue();
  this.function.accumulate(context,value);
}
