{
  ValueHandler result=caller.getFunction().execute(caller.getParameters(),context);
  if (result instanceof FunctionCaller) {
    result=resolveFunction((FunctionCaller)result,context);
  }
  return result;
}
