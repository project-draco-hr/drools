{
  String adjustedParam;
  if (boundParams.containsKey(param)) {
    adjustedParam=param;
  }
 else {
    adjustedParam=adjustParam(dataType,param,boundParams,isJavaDialect);
  }
  return adjustedParam;
}
