{
  final int SIZE=100;
  Cheese[] facts=new Cheese[SIZE];
  for (int i=0; i < facts.length; i++) {
    facts[i]=new Cheese();
    facts[i].setPrice(i);
    facts[i].setOldPrice(i);
  }
  return facts;
}
