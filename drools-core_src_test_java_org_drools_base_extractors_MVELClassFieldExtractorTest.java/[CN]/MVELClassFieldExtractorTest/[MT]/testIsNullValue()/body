{
  try {
    Assert.assertFalse(this.extractor.isNullValue(null,this.person));
    Extractor nullExtractor=ClassFieldExtractorCache.getInstance().getExtractor(Person.class,"addresses['business'].phone",getClass().getClassLoader());
    Assert.assertTrue(nullExtractor.isNullValue(null,this.person));
  }
 catch (  final Exception e) {
    fail("Should not throw an exception");
  }
}
