{
  try {
    Assert.assertFalse(this.extractor.isNullValue(this.person));
    Extractor nullExtractor=ClassFieldExtractorCache.getExtractor(Person.class,"addresses['business'].phone",getClass().getClassLoader());
    Assert.assertTrue(nullExtractor.isNullValue(this.person));
  }
 catch (  final Exception e) {
    fail("Should not throw an exception");
  }
}
