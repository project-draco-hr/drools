{
  try {
    Assert.assertEquals("Home Street",this.extractor.getValue(this.person));
    Assert.assertTrue(this.extractor.getValue(this.person) instanceof String);
  }
 catch (  final Exception e) {
    fail("Should not throw an exception");
  }
}
