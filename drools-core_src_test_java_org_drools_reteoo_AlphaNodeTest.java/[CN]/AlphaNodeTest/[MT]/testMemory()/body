{
  WorkingMemoryImpl workingMemory=new WorkingMemoryImpl(new RuleBaseImpl());
  AlphaNode alphaNode=new AlphaNode(2,null,true,null);
  Set memory=(HashSet)workingMemory.getNodeMemory(alphaNode);
  assertNotNull(memory);
}
