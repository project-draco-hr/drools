{
  if (constraint == null) {
    return true;
  }
  boolean isAllowed=false;
  try {
    isAllowed=isAllowed(workingMemory.getObject(handle),handle,tuple,workingMemory);
  }
 catch (  NoSuchFactObjectException e) {
  }
  return isAllowed;
}
