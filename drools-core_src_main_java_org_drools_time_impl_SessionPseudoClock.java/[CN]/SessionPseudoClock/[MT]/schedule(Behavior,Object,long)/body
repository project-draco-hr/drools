{
  ScheduledItem item=schedules.remove(behavior);
  if (item != null) {
    queue.remove(item);
  }
  item=new ScheduledItem(timestamp,behavior,behaviorContext);
  schedules.put(behavior,item);
  queue.add(item);
}
