{
  Class<?> declarationType=Map.class;
  Class<?> createType=HashMap.class;
  String variableName=getVariableName(fieldReader);
  return PRIVATE_MODIFIER + " " + declarationType.getName()+ " "+ variableName+ " = new "+ createType.getName()+ "();";
}
