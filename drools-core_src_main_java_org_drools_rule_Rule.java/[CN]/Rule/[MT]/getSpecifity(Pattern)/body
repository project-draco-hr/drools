{
  int specificity=0;
  for (final Iterator it=pattern.getConstraints().iterator(); it.hasNext(); ) {
    if (!(it.next() instanceof Declaration)) {
      specificity++;
    }
  }
  return specificity;
}
