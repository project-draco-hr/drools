{
  if (getDeclaration(identifier) != null) {
    throw new InvalidRuleException(this);
  }
  Declaration declaration=new Declaration(this.declarations.size(),identifier,objectType,extractor,column);
  this.declarations.put(identifier,declaration);
  return declaration;
}
