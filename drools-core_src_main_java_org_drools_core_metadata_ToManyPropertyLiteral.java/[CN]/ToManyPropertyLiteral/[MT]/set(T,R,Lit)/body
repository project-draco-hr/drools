{
switch (mode) {
case SET:
    set(o,Collections.singletonList(value));
  break;
case ADD:
List<R> list=get(o);
if (list == null) {
list=new ArrayList();
set(o,list);
}
list.add(value);
break;
case REMOVE:
List<R> curr=get(o);
if (curr != null) {
curr.remove(value);
}
break;
}
}
