{
  Collection objectTypeNodes=this.rete.getObjectTypeNodes();
  assertEquals(3,objectTypeNodes.size());
  assertContains(this.objectTypeNode,objectTypeNodes);
  assertContains(this.stringTypeNode,objectTypeNodes);
  assertContains(this.cheeseTypeNode,objectTypeNodes);
  ObjectTypeNode node=new ObjectTypeNode(0,new ClassObjectType(Map.class),null);
  assertTrue(objectTypeNodes.contains(node));
}
