{
  final InternalReadAccessor ex=store.getReader(TestAbstract.class,"something",getClass().getClassLoader());
  assertEquals(2,ex.getIndex());
  assertEquals("foo",ex.getValue(null,new TestAbstractImpl()));
}
