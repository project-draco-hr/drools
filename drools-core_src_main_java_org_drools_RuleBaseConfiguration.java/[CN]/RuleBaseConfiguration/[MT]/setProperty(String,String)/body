{
  if (!this.immutable) {
    if (PROPERTY_LOGICAL_ASSERT_BEHAVIOR.equals(prop) && WM_BEHAVIOR_IDENTITY.equals(value)) {
      this.properties.put(PROPERTY_ASSERT_BEHAVIOR,WM_BEHAVIOR_IDENTITY);
    }
    this.properties.put(prop,value);
  }
 else {
    throw new UnsupportedOperationException("Can't set a property after configuration becomes immutable");
  }
}
