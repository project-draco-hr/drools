{
  if (fact.isModify()) {
    return new ExistingFactPopulator(populatedData,typeResolver,classLoader,fact);
  }
 else {
    return new NewFactPopulator(populatedData,typeResolver,classLoader,fact);
  }
}
