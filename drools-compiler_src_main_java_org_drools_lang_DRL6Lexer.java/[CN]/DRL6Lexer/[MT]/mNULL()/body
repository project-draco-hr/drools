{
  try {
    int _type=NULL;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("null");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
