{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  CashFlow cashFlow=(CashFlow)o;
  if (type != cashFlow.type) {
    return false;
  }
  if (accountNo != cashFlow.accountNo) {
    return false;
  }
  if (amount != cashFlow.amount) {
    return false;
  }
  if (!date.equals(cashFlow.date)) {
    return false;
  }
  return true;
}
