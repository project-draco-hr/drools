{
  if (expression != null) {
    buf.append(" ");
    final ToStringExpressionVisitor visitor=new ToStringExpressionVisitor(expression.getBinding(),constraintValueBuilder);
    buf.append(expression.getText(visitor));
    buf.append(" ");
  }
}
