{
  String line=laDiscard();
  if (line == null) {
    return false;
  }
  Matcher matcher=IMPORT_STATEMENT.matcher(line);
  if (matcher.matches()) {
    consumeDiscard();
    imports.add(matcher.group(1));
    return true;
  }
  return false;
}
