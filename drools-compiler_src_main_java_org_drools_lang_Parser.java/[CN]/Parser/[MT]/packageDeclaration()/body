{
  String line=laDiscard();
  if (line == null) {
    return;
  }
  Matcher matcher=PACKAGE_DECL.matcher(line);
  if (matcher.matches()) {
    consumeDiscard();
    packageDeclaration=matcher.group(1);
  }
}
