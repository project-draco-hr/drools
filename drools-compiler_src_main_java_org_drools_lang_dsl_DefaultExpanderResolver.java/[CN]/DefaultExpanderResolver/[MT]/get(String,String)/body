{
  if (expanders.containsKey(name)) {
    return (Expander)expanders.get(name);
  }
 else {
    Expander exp=(Expander)expanders.get("*");
    if (exp == null)     throw new IllegalArgumentException("Unable to provide an expander for " + name + " or a default expander.");
    return exp;
  }
}
