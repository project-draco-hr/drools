{
  assertFalse(manager.isRunning());
  manager.startService();
  Thread.sleep(1000);
  assertTrue(manager.isRunning());
  manager.stopService();
  Thread.sleep(1000);
  assertFalse(manager.isRunning());
}
