{
  int ret=LoadOrderConflictResolver.getInstance().compare(o1,o2);
  if (ret == 0) {
    ret=(-1) * AbstractConflictResolver.compare(((RuleHandle)o1).getDominantPosition(),((RuleHandle)o2).getDominantPosition());
  }
  return ret;
}
