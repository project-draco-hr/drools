{
  int ret=LoadOrderConflictResolver.getInstance().compare(o1,o2);
  if (ret == 0) {
    ret=(-1) * AbstractConflictResolver.compare(((LeapsRuleHandle)o1).getDominantPosition(),((LeapsRuleHandle)o2).getDominantPosition());
  }
  return ret;
}
