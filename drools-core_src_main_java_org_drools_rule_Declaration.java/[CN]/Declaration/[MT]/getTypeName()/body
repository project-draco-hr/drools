{
  if (cachedTypeName == null) {
    cachedTypeName=(getExtractor() != null && getExtractor().getExtractToClass() != null) ? canonicalName(getExtractor().getExtractToClass()) : "java.lang.Object";
  }
  return cachedTypeName;
}
