{
  logger.info("registering kiescanner services");
  this.scannerReg=context.registerService(new String[]{KieScannerFactoryService.class.getName(),Service.class.getName()},new KieScannerFactoryServiceImpl(),new Hashtable());
  logger.info("kiescanner services registered");
}
