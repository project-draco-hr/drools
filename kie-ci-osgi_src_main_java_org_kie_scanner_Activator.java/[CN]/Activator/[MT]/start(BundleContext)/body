{
  logger.info("registering kiescanner services");
  this.scannerReg=context.registerService(new String[]{KieScanner.class.getName(),Service.class.getName()},new KieRepositoryScannerImpl(),new Hashtable());
  logger.info("kiescanner services registered");
}
