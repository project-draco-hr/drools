{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Holding holding=(Holding)o;
  if (!character.equals(holding.character)) {
    return false;
  }
  if (!thing.equals(holding.thing)) {
    return false;
  }
  return true;
}
