{
  this.object=evaluator.prepareLeftObject(handle);
  this.workingMemory=workingMemory;
  this.rightNull=this.extractor.isNullValue(workingMemory,evaluator.prepareRightObject(handle));
  if (!rightNull) {
    this.right=this.extractor.getBooleanValue(workingMemory,evaluator.prepareRightObject(handle));
  }
 else {
    this.right=false;
  }
}
