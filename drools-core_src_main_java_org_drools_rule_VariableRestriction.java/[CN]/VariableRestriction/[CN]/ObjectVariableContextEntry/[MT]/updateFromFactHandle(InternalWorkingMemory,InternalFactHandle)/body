{
  this.object=evaluator.prepareObject(handle);
  this.workingMemory=workingMemory;
  this.rightNull=this.extractor.isNullValue(workingMemory,evaluator.prepareObject(handle));
  this.right=this.extractor.getValue(workingMemory,evaluator.prepareObject(handle));
}
