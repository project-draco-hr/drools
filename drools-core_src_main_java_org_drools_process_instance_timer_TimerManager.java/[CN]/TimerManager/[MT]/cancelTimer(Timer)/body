{
  java.util.Timer utilTimer=utilTimers.get(timer);
  if (utilTimer == null) {
    throw new IllegalArgumentException("Could not find timer implementation for timer " + timer);
  }
  utilTimer.cancel();
  utilTimers.remove(timer);
  timers.remove(timer);
}
