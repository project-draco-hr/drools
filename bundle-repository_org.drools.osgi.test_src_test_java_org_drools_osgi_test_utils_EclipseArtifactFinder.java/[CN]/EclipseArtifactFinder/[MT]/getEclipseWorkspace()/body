{
  String workspaceAreaProp=System.getProperty(s_PROP_WORKSPACE_AREA,"../..");
  System.out.println("workspace area: " + workspaceAreaProp);
  if (workspaceAreaProp != null)   return new File(workspaceAreaProp);
 else   return null;
}
