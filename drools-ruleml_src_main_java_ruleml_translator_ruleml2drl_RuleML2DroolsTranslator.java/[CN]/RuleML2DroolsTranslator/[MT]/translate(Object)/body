{
  if (!(o instanceof String)) {
    throw new IllegalArgumentException("The type of the object to translate is not String");
  }
  String input=(String)o;
  RuleMLType ruleML=createRuleMLFromXML(input);
  currentProcessor.setTranslator(this);
  dispatchType(ruleML);
  getDrl().setPackage_("org.ruleml.translator");
  getDrl().setImports(new String[]{"org.ruleml.translator.TestDataModel.*"});
  return getDrl().toString();
}
