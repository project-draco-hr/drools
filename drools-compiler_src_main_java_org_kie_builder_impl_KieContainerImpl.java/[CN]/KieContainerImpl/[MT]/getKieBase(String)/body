{
  KieBase kBase=kBases.get(kBaseName);
  if (kBase == null) {
    kBase=AbstractKieModules.createKieBase(kProject.getKieBaseModel(kBaseName),kProject);
    if (kBase != null) {
      kBases.put(kBaseName,kBase);
    }
  }
  return kBase;
}
