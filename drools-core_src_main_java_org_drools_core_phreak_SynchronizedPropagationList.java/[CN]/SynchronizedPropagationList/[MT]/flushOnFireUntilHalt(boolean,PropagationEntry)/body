{
  if (!fired && currentHead == null) {
synchronized (this) {
      if (head == null) {
        halt();
      }
      currentHead=takeAll();
    }
  }
  while (currentHead != null) {
    flush(workingMemory,currentHead);
    currentHead=takeAll();
  }
}
