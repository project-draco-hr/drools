{
  LispForm t=null;
  try {
{
      match(input,EQUALS,FOLLOW_EQUALS_in_return_value_restriction1385);
      if (failed)       return;
      pushFollow(FOLLOW_lisp_form_in_return_value_restriction1392);
      t=lisp_form();
      _fsp--;
      if (failed)       return;
      if (backtracking == 0) {
        rc.addRestriction(new ReturnValueRestrictionDescr(op,t));
      }
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return;
}
