{
  LispForm t=null;
  try {
{
      match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_eval_ce770);
      if (state.failed)       return;
      match(input,TEST,FOLLOW_TEST_in_eval_ce775);
      if (state.failed)       return;
      pushFollow(FOLLOW_lisp_form_in_eval_ce782);
      t=lisp_form();
      state._fsp--;
      if (state.failed)       return;
      if (state.backtracking == 0) {
        EvalDescr evalDescr=new EvalDescr();
        evalDescr.setContent(t);
        in_ce.addDescr(evalDescr);
      }
      match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_eval_ce792);
      if (state.failed)       return;
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return;
}
