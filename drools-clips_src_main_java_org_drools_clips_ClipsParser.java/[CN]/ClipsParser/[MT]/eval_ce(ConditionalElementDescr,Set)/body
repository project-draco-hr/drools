{
  LispForm t=null;
  try {
{
      match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_eval_ce941);
      if (failed)       return;
      match(input,TEST,FOLLOW_TEST_in_eval_ce946);
      if (failed)       return;
      pushFollow(FOLLOW_lisp_form_in_eval_ce953);
      t=lisp_form();
      _fsp--;
      if (failed)       return;
      if (backtracking == 0) {
        EvalDescr evalDescr=new EvalDescr();
        evalDescr.setContent(t);
        in_ce.addDescr(evalDescr);
      }
      match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_eval_ce963);
      if (failed)       return;
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return;
}
