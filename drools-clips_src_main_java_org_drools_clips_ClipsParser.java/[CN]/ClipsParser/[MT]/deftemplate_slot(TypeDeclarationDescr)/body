{
  Token slotName=null;
  Token slotType=null;
  try {
{
      match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_deftemplate_slot266);
      if (failed)       return;
      match(input,SLOT,FOLLOW_SLOT_in_deftemplate_slot273);
      if (failed)       return;
      slotName=(Token)input.LT(1);
      match(input,NAME,FOLLOW_NAME_in_deftemplate_slot277);
      if (failed)       return;
      match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_deftemplate_slot287);
      if (failed)       return;
      match(input,TYPE,FOLLOW_TYPE_in_deftemplate_slot298);
      if (failed)       return;
      slotType=(Token)input.LT(1);
      match(input,NAME,FOLLOW_NAME_in_deftemplate_slot302);
      if (failed)       return;
      match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_deftemplate_slot312);
      if (failed)       return;
      if (backtracking == 0) {
        typeDescr.addField(new TypeFieldDescr(slotName.getText(),new PatternDescr(slotType.getText())));
      }
      match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_deftemplate_slot320);
      if (failed)       return;
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return;
}
