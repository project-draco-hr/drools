{
  Token name=null;
  PatternDescr pattern=null;
  ConditionalElementDescr top=null;
  try {
{
      match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_normal_pattern820);
      if (state.failed)       return;
      name=(Token)match(input,NAME,FOLLOW_NAME_in_normal_pattern827);
      if (state.failed)       return;
      if (state.backtracking == 0) {
        pattern=new PatternDescr(name.getText());
        in_ce.addDescr(pattern);
        top=pattern.getConstraint();
      }
      loop10:       do {
        int alt10=2;
        int LA10_0=input.LA(1);
        if ((LA10_0 == LEFT_PAREN)) {
          alt10=1;
        }
switch (alt10) {
case 1:
{
            pushFollow(FOLLOW_field_constriant_in_normal_pattern833);
            field_constriant(top,declarations);
            state._fsp--;
            if (state.failed)             return;
          }
        break;
default :
      break loop10;
  }
}
 while (true);
match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_normal_pattern843);
if (state.failed) return;
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
