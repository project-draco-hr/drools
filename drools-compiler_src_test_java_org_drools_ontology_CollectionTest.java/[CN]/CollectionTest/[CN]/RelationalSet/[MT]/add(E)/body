{
  ManyToManyRelation relation=new ManyToManyRelation(this.object,o);
  if (!this.map.containsKey(o)) {
    this.map.put(o,relation);
    relation.setFactHandle(kruntime.insert(relation));
    return true;
  }
 else {
    return false;
  }
}
