{
  Activation activation=afteractivationfiredevent.getActivation();
  String debugStr="==>[afterActivationFired(" + activation.getPropagationContext().getPropagationNumber() + "): rule="+ activation.getRule().getName()+ ";]";
}
