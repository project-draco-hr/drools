{
  Activation activation=activationcancelledevent.getActivation();
  String debugStr="==>[activationCancelled(" + activation.getPropagationContext().getPropagationNumber() + "): rule="+ activation.getRule().getName()+ ";]";
}
