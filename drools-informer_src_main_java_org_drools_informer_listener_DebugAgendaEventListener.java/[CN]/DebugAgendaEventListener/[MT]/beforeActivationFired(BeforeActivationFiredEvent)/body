{
  Activation activation=beforeactivationfiredevent.getActivation();
  String debugStr="==>[beforeActivationFired(" + activation.getPropagationContext().getPropagationNumber() + "): rule="+ activation.getRule().getName()+ ";]";
}
