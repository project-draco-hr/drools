{
  final Map values=new HashMap();
  this.processNL(nl,values);
  final String subKey=this.getSubstitutionKey();
  final String target=this.populateTargetString(values,targetTemplate);
  return this.interpolate(nl,subKey,target);
}
