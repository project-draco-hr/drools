{
  Map<Resource,ResourceType> resources=new HashMap<Resource,ResourceType>();
  for (int i=0; i < defaultList.length; i++) {
    resources.put(ResourceFactory.newClassPathResource(defaultList[i],Verifier.class),ResourceType.DRL);
  }
  return resources;
}
