{
  final FieldValue value=FieldFactory.getFieldValue(row[2]);
  RuntimeDroolsException exc=null;
  try {
    final boolean result=evaluator.evaluate(null,extractor,row[0],value);
  }
 catch (  RuntimeDroolsException e) {
    exc=e;
  }
  assertNotNull(exc);
}
