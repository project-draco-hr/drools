{
switch (valueType) {
case BOOLEAN:
    return Boolean.valueOf(booleanValue);
case DATE:
  return dateValue;
case DOUBLE:
return Double.valueOf(doubleValue);
case INT:
return Integer.valueOf(intValue);
default :
return stringValue;
}
}
