{
  if (!this.factQueue.isEmpty()) {
    propagateQueuedActions();
  }
  if (!this.firing) {
    try {
      this.firing=true;
      while (this.agenda.fireNextItem(agendaFilter)) {
        ;
      }
    }
  finally {
      this.firing=false;
    }
  }
}
