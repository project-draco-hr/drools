{
  String encoding;
  encoding=((ElemExtensionCall)elem).getAttribute("encoding",context.getContextNode(),context.getTransformer());
  if (encoding == null) {
    return "";
  }
 else {
    return encoding;
  }
}
