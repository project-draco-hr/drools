{
  Map variables=new HashMap();
  variables.put("yyy",String.class);
  variables.put("iii",String.class);
  assertEquals("org.drools.Func.func(yyy, iii)",fixer.fix("func(yyy, iii)",new DeclarationScopeResolver(new Map[]{variables})));
}
