{
  boolean value=false;
switch (this.type) {
case ConstraintComparator.EQUAL:
    value=isEqual(object1,object2);
  break;
case ConstraintComparator.NOT_EQUAL:
value=isNotEqual(object1,object2);
break;
}
return value;
}
