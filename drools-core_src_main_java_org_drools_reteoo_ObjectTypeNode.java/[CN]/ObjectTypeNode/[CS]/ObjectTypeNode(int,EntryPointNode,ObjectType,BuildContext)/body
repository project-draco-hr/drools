{
  super(id,context.getPartitionId(),context.getRuleBase().getConfiguration().isPartitionsEnabled(),source,context.getRuleBase().getConfiguration().getAlphaNodeHashingThreshold());
  this.objectType=objectType;
  setObjectMemoryEnabled(context.isObjectTypeNodeMemoryEnabled());
}
