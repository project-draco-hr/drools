{
  int explicitGenericInvocationSuffix_StartIndex=input.index();
  try {
    if (state.backtracking > 0 && alreadyParsedRule(input,129)) {
      return;
    }
    int alt174=2;
    int LA174_0=input.LA(1);
    if ((LA174_0 == 64)) {
      alt174=1;
    }
 else     if ((LA174_0 == Identifier)) {
      alt174=2;
    }
 else {
      if (state.backtracking > 0) {
        state.failed=true;
        return;
      }
      NoViableAltException nvae=new NoViableAltException("",174,0,input);
      throw nvae;
    }
switch (alt174) {
case 1:
{
        match(input,64,FOLLOW_64_in_explicitGenericInvocationSuffix4333);
        if (state.failed)         return;
        pushFollow(FOLLOW_superSuffix_in_explicitGenericInvocationSuffix4335);
        superSuffix();
        state._fsp--;
        if (state.failed)         return;
      }
    break;
case 2:
{
    match(input,Identifier,FOLLOW_Identifier_in_explicitGenericInvocationSuffix4342);
    if (state.failed)     return;
    pushFollow(FOLLOW_arguments_in_explicitGenericInvocationSuffix4344);
    arguments();
    state._fsp--;
    if (state.failed)     return;
  }
break;
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
if (state.backtracking > 0) {
memoize(input,129,explicitGenericInvocationSuffix_StartIndex);
}
}
return;
}
