{
  int explicitGenericInvocationSuffix_StartIndex=input.index();
  try {
    if (backtracking > 0 && alreadyParsedRule(input,128)) {
      return;
    }
    int alt174=2;
    int LA174_0=input.LA(1);
    if ((LA174_0 == 64)) {
      alt174=1;
    }
 else     if ((LA174_0 == Identifier)) {
      alt174=2;
    }
 else {
      if (backtracking > 0) {
        failed=true;
        return;
      }
      NoViableAltException nvae=new NoViableAltException("936:1: explicitGenericInvocationSuffix : ( 'super' superSuffix | Identifier arguments );",174,0,input);
      throw nvae;
    }
switch (alt174) {
case 1:
{
        match(input,64,FOLLOW_64_in_explicitGenericInvocationSuffix4269);
        if (failed)         return;
        pushFollow(FOLLOW_superSuffix_in_explicitGenericInvocationSuffix4271);
        superSuffix();
        _fsp--;
        if (failed)         return;
      }
    break;
case 2:
{
    match(input,Identifier,FOLLOW_Identifier_in_explicitGenericInvocationSuffix4278);
    if (failed)     return;
    pushFollow(FOLLOW_arguments_in_explicitGenericInvocationSuffix4280);
    arguments();
    _fsp--;
    if (failed)     return;
  }
break;
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
if (backtracking > 0) {
memoize(input,128,explicitGenericInvocationSuffix_StartIndex);
}
}
return;
}
