{
  int defaultValue_StartIndex=input.index();
  try {
    if (backtracking > 0 && alreadyParsedRule(input,79)) {
      return;
    }
{
      match(input,72,FOLLOW_72_in_defaultValue2201);
      if (failed)       return;
      pushFollow(FOLLOW_elementValue_in_defaultValue2203);
      elementValue();
      _fsp--;
      if (failed)       return;
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
    if (backtracking > 0) {
      memoize(input,79,defaultValue_StartIndex);
    }
  }
  return;
}
