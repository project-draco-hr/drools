{
  GamePanel sensorPanel=new GamePanel("sensor",Color.WHITE);
  FlowLayout flowLayout=(FlowLayout)sensorPanel.getLayout();
  flowLayout.setVgap(10);
  sensorPanel.setBackground(Color.WHITE);
  return sensorPanel;
}
