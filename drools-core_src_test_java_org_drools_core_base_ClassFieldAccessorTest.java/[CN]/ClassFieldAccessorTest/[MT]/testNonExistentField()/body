{
  final Object[] objArray=new Object[1];
  final TestBean obj=new TestBean();
  obj.setBlah(false);
  obj.setSomething("no");
  obj.setObjArray(objArray);
  try {
    final ClassFieldReader ext=store.getReader(TestBean.class,"xyz",getClass().getClassLoader());
    fail("A RuntimeException should have been raised");
  }
 catch (  RuntimeException e) {
  }
catch (  Exception e) {
    fail("A RuntimeException should have been raised");
  }
}
