{
  checkRuleSessionValidity();
  if (objectHandle instanceof FactHandle) {
    getWorkingMemory().modifyObject((FactHandle)objectHandle,newObject);
  }
 else {
    throw new InvalidHandleException("invalid handle");
  }
}
