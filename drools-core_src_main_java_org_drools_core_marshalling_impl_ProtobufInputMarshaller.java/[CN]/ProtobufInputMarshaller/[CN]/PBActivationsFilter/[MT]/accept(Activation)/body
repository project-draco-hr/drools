{
  ActivationKey key=PersisterHelper.createActivationKey(match.getRule().getPackageName(),match.getRule().getName(),match.getTuple());
  this.tuplesCache.put(key,match.getTuple());
  return !this.dormantActivations.containsKey(key);
}
