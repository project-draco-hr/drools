{
  super(conflictResolver.getFactConflictResolver());
  positiveRules=new RuleTable(conflictResolver.getRuleConflictResolver());
  negativeRules=new RuleTable(conflictResolver.getRuleConflictResolver());
}
