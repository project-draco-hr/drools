{
  TableIterator ret=((HashedTableComponent)this.notAndExistsHashedTables.get(constraint)).iteratorFromPositionToTableEnd(tuple,startFactHandle);
  if (ret == null) {
    ret=Table.emptyIterator();
  }
  return ret;
}
