{
  String contents=(String)registry.getTemplate(templateName);
  if (contents == null) {
    RuleTemplate template=(RuleTemplate)ruleTemplates.get(templateName);
    contents=template.getContents();
    registry.registerTemplate(templateName,contents);
  }
  return contents;
}
