{
  try {
    int _type=ENUM;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("enum");
      if (!enumIsKeyword)       _type=Identifier;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
