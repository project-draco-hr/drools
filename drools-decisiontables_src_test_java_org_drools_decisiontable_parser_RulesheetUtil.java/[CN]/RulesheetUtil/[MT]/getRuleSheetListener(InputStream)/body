{
  Map listners=new HashMap();
  RuleSheetListener listener=new RuleSheetListener();
  listners.put(ExcelParser.DEFAULT_RULESHEET_NAME,listener);
  ExcelParser parser=new ExcelParser(listners);
  parser.parseFile(stream);
  stream.close();
  return listener;
}
