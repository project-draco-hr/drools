{
  final DefaultFactHandle f0=(DefaultFactHandle)this.workingMemory.assertObject("test0");
  this.node.assertObject(f0,this.context,this.workingMemory);
  assertEquals(0,this.memory.getLeftTupleMemory().size());
  assertEquals(1,this.memory.getRightObjectMemory().size());
  final DefaultFactHandle f1=(DefaultFactHandle)this.workingMemory.assertObject("test1");
  this.node.assertObject(f1,this.context,this.workingMemory);
  assertEquals(2,this.memory.getRightObjectMemory().size());
  final BetaRightMemory rightMemory=this.memory.getRightObjectMemory();
  final Iterator it=rightMemory.iterator(this.workingMemory,new ReteTuple(f0));
  final DefaultFactHandle rf0=((ObjectMatches)it.next()).getFactHandle();
  final DefaultFactHandle rf1=((ObjectMatches)it.next()).getFactHandle();
  assertEquals(f0,rf0);
  assertEquals(f1,rf1);
}
