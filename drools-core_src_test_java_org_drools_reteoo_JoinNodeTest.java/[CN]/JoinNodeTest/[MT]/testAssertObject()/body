{
  FactHandleImpl f0=(FactHandleImpl)this.workingMemory.assertObject("test0");
  this.node.assertObject(f0,this.context,this.workingMemory);
  assertEquals(0,this.memory.getLeftTupleMemory().size());
  assertEquals(1,this.memory.getRightFactHandleMemory().size());
  FactHandleImpl f1=(FactHandleImpl)this.workingMemory.assertObject("test1");
  this.node.assertObject(f1,this.context,this.workingMemory);
  assertEquals(2,this.memory.getRightFactHandleMemory().size());
  Map map=this.memory.getRightFactHandleMemory();
  assertContains(f0,map.keySet());
  assertContains(f1,map.keySet());
}
