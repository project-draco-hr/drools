{
  FactHandleImpl f0=new FactHandleImpl(0);
  ReteTuple tuple0=new ReteTuple(f0);
  this.node.assertTuple(tuple0,this.context,this.workingMemory);
  assertEquals(1,this.memory.getLeftTupleMemory().size());
  assertEquals(0,this.memory.getRightFactHandleMemory().size());
  FactHandleImpl f1=new FactHandleImpl(1);
  ReteTuple tuple1=new ReteTuple(f1);
  this.node.assertTuple(tuple1,this.context,this.workingMemory);
  assertEquals(2,this.memory.getLeftTupleMemory().size());
  ReteTuple tuple=this.memory.getFirstTuple();
  assertEquals(tuple0,tuple);
  assertEquals(tuple1,tuple.getNext());
}
