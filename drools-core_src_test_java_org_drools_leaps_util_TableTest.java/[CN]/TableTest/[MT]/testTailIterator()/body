{
  this.testTable.add(this.h1);
  this.testTable.add(this.h1000);
  this.testTable.add(this.h100);
  this.testTable.add(this.h10);
  TableIterator it=this.testTable.tailIterator(this.h100,this.h10);
  assertTrue(it.hasNext());
  assertEquals(it.next(),this.h10);
  assertTrue(it.hasNext());
  assertEquals(it.next(),this.h1);
  assertFalse(it.hasNext());
  it.reset();
  assertTrue(it.hasNext());
  assertEquals(it.next(),this.h100);
  assertTrue(it.hasNext());
  assertEquals(it.next(),this.h10);
  assertTrue(it.hasNext());
  assertEquals(it.next(),this.h1);
  assertFalse(it.hasNext());
  this.testTable.clear();
  final DefaultFactHandle fh1=new DefaultFactHandle(1,new Guest("1",Sex.resolve("m"),Hobby.resolve("h2")));
  final DefaultFactHandle fh2=new DefaultFactHandle(2,new Guest("1",Sex.resolve("m"),Hobby.resolve("h1")));
  final DefaultFactHandle fh3=new DefaultFactHandle(3,new Guest("1",Sex.resolve("m"),Hobby.resolve("h3")));
  final DefaultFactHandle fh4=new DefaultFactHandle(4,new Guest("3",Sex.resolve("f"),Hobby.resolve("h2")));
  final DefaultFactHandle fhC=new DefaultFactHandle(5,new Context("start"));
  this.testTable.add(fh1);
  this.testTable.add(fh2);
  this.testTable.add(fh3);
  this.testTable.add(fh4);
  it=this.testTable.tailIterator(fhC,fhC);
  assertTrue(it.hasNext());
  assertEquals(it.next(),fh4);
}
