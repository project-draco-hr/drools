{
  if (!(in instanceof StateItem)) {
    return false;
  }
 else   if (in == this) {
    return true;
  }
 else {
    StateItem other=(StateItem)in;
    return this.getName().equals(other.getName());
  }
}
