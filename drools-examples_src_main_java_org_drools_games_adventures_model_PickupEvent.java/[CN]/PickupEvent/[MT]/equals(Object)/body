{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  PickupEvent that=(PickupEvent)o;
  if (!character.equals(that.character)) {
    return false;
  }
  if (!thing.equals(that.thing)) {
    return false;
  }
  return true;
}
