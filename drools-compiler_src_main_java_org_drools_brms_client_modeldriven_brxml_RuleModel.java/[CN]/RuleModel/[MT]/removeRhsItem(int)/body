{
  IAction[] newList=new IAction[rhs.length - 1];
  int newIdx=0;
  for (int i=0; i < rhs.length; i++) {
    if (i != idx) {
      newList[newIdx]=rhs[i];
      newIdx++;
    }
  }
  this.rhs=newList;
}
