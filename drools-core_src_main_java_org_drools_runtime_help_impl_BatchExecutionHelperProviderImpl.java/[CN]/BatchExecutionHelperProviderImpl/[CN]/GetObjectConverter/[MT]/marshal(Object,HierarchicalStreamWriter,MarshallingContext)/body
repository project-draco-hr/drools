{
  GetObjectCommand cmd=(GetObjectCommand)object;
  writer.addAttribute("factHandle",cmd.getFactHandle().toExternalForm());
  if (cmd.getOutIdentifier() != null) {
    writer.addAttribute("out-identifier",cmd.getOutIdentifier());
  }
}
