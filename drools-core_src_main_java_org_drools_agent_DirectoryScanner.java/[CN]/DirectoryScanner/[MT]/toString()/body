{
  String s="DirectoryScanner";
  if (dir != null) {
    s=s + " scanning dir: " + dir.getPath();
  }
  if (currentList != null) {
    s=s + " found " + currentList.length+ " file(s).";
  }
  return s;
}
