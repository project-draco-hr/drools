{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ConstraintsPair that=(ConstraintsPair)o;
  return (constraints1.equals(that.constraints1) && constraints2.equals(that.constraints2)) || (constraints1.equals(that.constraints2) && constraints2.equals(that.constraints1));
}
