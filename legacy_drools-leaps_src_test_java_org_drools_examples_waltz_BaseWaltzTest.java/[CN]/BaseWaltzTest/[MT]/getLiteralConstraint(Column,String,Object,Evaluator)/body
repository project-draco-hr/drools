{
  final Class clazz=((ClassObjectType)column.getObjectType()).getClassType();
  final FieldExtractor extractor=ClassFieldExtractorFactory.getClassFieldExtractor(clazz,fieldName);
  final FieldValue field=FieldFactory.getFieldValue(fieldValue);
  return new LiteralConstraint(extractor,evaluator,field);
}
