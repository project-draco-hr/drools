{
  String returnType="()" + Type.getDescriptor(returnClass);
  mv.visitLabel(label);
  mv.visitVarInsn(ALOAD,target);
  mv.visitMethodInsn(INVOKEVIRTUAL,targetType,targetMethod,returnType);
  mv.visitInsn(ARETURN);
}
