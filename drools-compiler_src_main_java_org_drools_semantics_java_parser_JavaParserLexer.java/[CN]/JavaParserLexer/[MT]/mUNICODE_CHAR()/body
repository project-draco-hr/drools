{
  try {
    ruleNestingLevel++;
{
      match('\\');
      match('u');
      mHEX_DIGIT();
      mHEX_DIGIT();
      mHEX_DIGIT();
      mHEX_DIGIT();
    }
  }
  finally {
    ruleNestingLevel--;
  }
}
