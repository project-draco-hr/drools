{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  if (!super.equals(o)) {
    return false;
  }
  Door door=(Door)o;
  if (!fromRoom.equals(door.fromRoom)) {
    return false;
  }
  if (!toRoom.equals(door.toRoom)) {
    return false;
  }
  return true;
}
