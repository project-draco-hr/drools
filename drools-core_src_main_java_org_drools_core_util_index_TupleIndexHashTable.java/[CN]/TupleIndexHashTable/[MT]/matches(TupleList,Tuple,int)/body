{
  if (list.hashCode() != tupleHashCode) {
    return false;
  }
  return left ? this.index.equal(tuple.getFactHandle().getObject(),list.getFirst()) : this.index.equal(list.getFirst().getFactHandle().getObject(),tuple);
}
