{
  if (list.hashCode() != tupleHashCode) {
    return false;
  }
  return left ? this.index.equal(list.getFirst(),tuple) : this.index.equal(list.getFirst().getFactHandle().getObject(),tuple.getFactHandle().getObject());
}
