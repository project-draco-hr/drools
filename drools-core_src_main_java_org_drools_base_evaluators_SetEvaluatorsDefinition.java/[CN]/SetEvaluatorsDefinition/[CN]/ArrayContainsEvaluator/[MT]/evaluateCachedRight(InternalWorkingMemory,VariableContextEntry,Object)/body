{
  final Object array=((ObjectVariableContextEntry)context).right;
  if (array == null)   return false;
  return primitiveArrayEvaluator.get(array.getClass()).contains(array,workingMemory,context.declaration.getExtractor(),left,true);
}
