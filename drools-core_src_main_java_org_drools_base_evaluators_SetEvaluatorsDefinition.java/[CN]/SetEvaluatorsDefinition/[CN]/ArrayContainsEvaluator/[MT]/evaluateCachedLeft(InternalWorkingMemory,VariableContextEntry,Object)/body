{
  final Object array=context.extractor.getValue(workingMemory,right);
  if (array == null)   return false;
  return primitiveArrayEvaluator.get(array.getClass()).contains(array,workingMemory,context.declaration.getExtractor(),context.getTuple().get(context.getVariableDeclaration()).getObject(),true);
}
