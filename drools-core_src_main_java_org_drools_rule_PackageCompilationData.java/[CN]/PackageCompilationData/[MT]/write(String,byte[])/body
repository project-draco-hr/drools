{
  if (store.put(resourceName.replace('.','/') + ".class",clazzData) != null) {
    reload();
  }
 else {
    try {
      wire(resourceName);
    }
 catch (    Exception e) {
      throw new RuntimeDroolsException(e);
    }
  }
}
