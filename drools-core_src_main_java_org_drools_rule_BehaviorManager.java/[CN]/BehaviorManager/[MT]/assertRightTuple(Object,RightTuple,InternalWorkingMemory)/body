{
  boolean result=true;
  for (int i=0; i < behaviors.length; i++) {
    result=result && behaviors[i].assertRightTuple(((Object[])behaviorContext)[i],rightTuple,workingMemory);
  }
  return result;
}
