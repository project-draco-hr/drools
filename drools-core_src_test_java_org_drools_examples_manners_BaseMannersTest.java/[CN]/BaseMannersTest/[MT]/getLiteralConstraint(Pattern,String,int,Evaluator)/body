{
  final Class clazz=((ClassObjectType)pattern.getObjectType()).getClassType();
  final FieldExtractor extractor=ClassFieldExtractorCache.getExtractor(clazz,fieldName,getClass().getClassLoader());
  final FieldValue field=new LongFieldImpl(fieldValue);
  return new LiteralConstraint(extractor,evaluator,field);
}
