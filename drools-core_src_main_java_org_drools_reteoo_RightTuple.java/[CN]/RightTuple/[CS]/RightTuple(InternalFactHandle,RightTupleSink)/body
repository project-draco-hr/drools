{
  this.handle=handle;
  this.sink=sink;
  RightTuple last=handle.getLastRightTuple();
  if (last == null) {
    handle.addFirstRightTuple(this);
  }
 else {
    handle.addLastRightTuple(this);
  }
}
