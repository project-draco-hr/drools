{
  try {
    int _type=OR;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("or");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
