{
  try {
    int _type=DEFFUNCTION;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("deffunction");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
