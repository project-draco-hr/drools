{
  try {
    int _type=DEFRULE;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("defrule");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
