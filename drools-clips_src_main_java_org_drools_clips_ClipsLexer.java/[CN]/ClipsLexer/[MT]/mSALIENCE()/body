{
  try {
    int _type=SALIENCE;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("salience");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
