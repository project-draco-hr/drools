{
  try {
    int _type=DEFTEMPLATE;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("deftemplate");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
