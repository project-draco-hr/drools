{
  try {
    int _type=DECLARE;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("declare");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
