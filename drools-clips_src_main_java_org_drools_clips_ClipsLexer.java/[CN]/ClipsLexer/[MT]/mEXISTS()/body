{
  try {
    int _type=EXISTS;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("exists");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
