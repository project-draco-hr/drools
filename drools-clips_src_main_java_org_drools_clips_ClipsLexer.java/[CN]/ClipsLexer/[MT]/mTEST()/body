{
  try {
    int _type=TEST;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("test");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
