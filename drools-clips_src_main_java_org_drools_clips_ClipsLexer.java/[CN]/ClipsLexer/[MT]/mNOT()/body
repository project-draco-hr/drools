{
  try {
    int _type=NOT;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("not");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
