{
  try {
    int _type=NAME;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      mSYMBOL();
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
