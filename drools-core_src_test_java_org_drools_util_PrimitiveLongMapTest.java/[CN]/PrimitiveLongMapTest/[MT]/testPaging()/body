{
  PrimitiveLongMap map=new PrimitiveLongMap(32,8);
  for (int i=0; i < 512; i++) {
    Integer value=new Integer(i);
    Object oldValue=map.put(i,value);
    assertNull("OldValueNullTest ",oldValue);
    assertEquals("OldValueNullTest ",value,map.get(i));
  }
}
