{
  if (!existsVariableWithSameName(varname))   return varname + " : ";
  for (int count=1; count <= 100; count++) {
    if (!existsVariableWithSameName(varname + count))     return varname + count + " : ";
  }
  return varname;
}
