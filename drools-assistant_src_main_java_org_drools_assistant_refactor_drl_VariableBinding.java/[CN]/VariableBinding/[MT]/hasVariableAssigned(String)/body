{
  for (int position=line.length() - 1; position >= 0; position--) {
    if (line.charAt(position) == ':')     return true;
    if (line.charAt(position) == ',' || line.charAt(position) == '(')     return false;
  }
  return false;
}
