{
  LeftTupleSink sink=sinks.get(i);
  if (i == 0) {
    leftTuples.add(lt);
  }
 else {
    LeftTuple peer=lt;
    while (peer != null && peer.getSink() != sink) {
      peer=peer.getPeer();
    }
    if (peer != null) {
      followPeer(leftTuples,lt,sinks,i - 1,wm);
    }
  }
}
