{
  TableIterator ret;
  if (this.empty) {
    ret=new BaseTableReverseOrderIterator(null,null,null);
  }
 else {
    ret=new BaseTableReverseOrderIterator(this.tailRecord,this.tailRecord,this.headRecord);
  }
  return ret;
}
