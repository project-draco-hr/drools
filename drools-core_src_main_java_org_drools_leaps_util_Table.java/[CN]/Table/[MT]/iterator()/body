{
  TableIterator ret;
  if (this.empty) {
    ret=new BaseTableIterator(null,null,null);
  }
 else {
    ret=new BaseTableIterator(this.headRecord,this.headRecord,this.tailRecord);
  }
  return ret;
}
