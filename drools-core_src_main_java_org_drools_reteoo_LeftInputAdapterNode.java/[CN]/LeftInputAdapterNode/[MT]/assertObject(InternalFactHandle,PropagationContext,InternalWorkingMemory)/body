{
  final ReteTuple tuple=this.sink.createAndPropagateAssertTuple(handle,context,workingMemory);
  if (this.hasMemory) {
    final ObjectHashMap map=(ObjectHashMap)workingMemory.getNodeMemory(this);
    map.put(handle,tuple,false);
  }
}
