{
  ReteTuple tuple=this.sink.createAndPropagateAssertTuple(handle,context,workingMemory);
  if (this.hasMemory) {
    ObjectHashMap map=(ObjectHashMap)workingMemory.getNodeMemory(this);
    map.put(handle,tuple,false);
  }
}
