{
  LiaNodeMemory memory=(LiaNodeMemory)workingMemory.getNodeMemory(node);
  if (memory.getSegmentMemory() == null) {
    BetaNode.createNodeSegmentMemory(node,workingMemory);
  }
  node.getSinkPropagator().createAndPropagateAssertLeftTuple(handle,context,workingMemory,false,node);
}
