{
  FactHandle handle=getIdentityMap().get(object);
  ((InternalWorkingMemoryEntryPoint)((InternalFactHandle)handle).getEntryPoint()).modifyInsert(handle,object,rule,activation);
  this.getIdentityMap().put(object,handle);
}
