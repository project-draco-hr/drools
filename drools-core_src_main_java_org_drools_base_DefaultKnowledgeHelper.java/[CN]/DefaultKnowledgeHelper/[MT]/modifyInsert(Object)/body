{
  FactHandle handle=getFactHandle(object);
  if (handle == null) {
    throw new FactException("Modify error: handle not found for object: " + object + ". Is it in the working memory?");
  }
  modifyInsert(handle,object);
}
