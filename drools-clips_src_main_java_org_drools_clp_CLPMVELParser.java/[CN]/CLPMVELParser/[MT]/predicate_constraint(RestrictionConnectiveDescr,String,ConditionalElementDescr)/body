{
  SExpression t=null;
  try {
{
      match(input,COLON,FOLLOW_COLON_in_predicate_constraint965);
      if (failed)       return;
      pushFollow(FOLLOW_lisp_list_in_predicate_constraint971);
      t=lisp_list();
      _fsp--;
      if (failed)       return;
      if (backtracking == 0) {
        rc.addRestriction(new PredicateDescr(t));
      }
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return;
}
