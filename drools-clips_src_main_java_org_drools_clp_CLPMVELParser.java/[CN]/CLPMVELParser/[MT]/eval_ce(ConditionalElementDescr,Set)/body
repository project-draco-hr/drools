{
  SExpression t=null;
  try {
{
      match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_eval_ce546);
      if (failed)       return;
      match(input,TEST,FOLLOW_TEST_in_eval_ce551);
      if (failed)       return;
      pushFollow(FOLLOW_lisp_list_in_eval_ce558);
      t=lisp_list();
      _fsp--;
      if (failed)       return;
      if (backtracking == 0) {
        EvalDescr evalDescr=new EvalDescr();
        evalDescr.setContent(t);
        in_ce.addDescr(evalDescr);
      }
      match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_eval_ce568);
      if (failed)       return;
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return;
}
