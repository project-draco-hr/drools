{
  Token name=null;
  PatternDescr pattern=null;
  ConditionalElementDescr top=null;
  try {
{
      match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_normal_pattern596);
      if (failed)       return;
      name=(Token)input.LT(1);
      match(input,NAME,FOLLOW_NAME_in_normal_pattern603);
      if (failed)       return;
      if (backtracking == 0) {
        pattern=new PatternDescr(name.getText());
        in_ce.addDescr(pattern);
        top=pattern.getConstraint();
      }
      loop7:       do {
        int alt7=2;
        int LA7_0=input.LA(1);
        if ((LA7_0 == LEFT_PAREN)) {
          alt7=1;
        }
switch (alt7) {
case 1:
{
            pushFollow(FOLLOW_field_constriant_in_normal_pattern609);
            field_constriant(top,declarations);
            _fsp--;
            if (failed)             return;
          }
        break;
default :
      break loop7;
  }
}
 while (true);
match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_normal_pattern619);
if (failed) return;
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
