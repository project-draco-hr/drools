{
  final List<List<TestOperation>> testPlan=AddRemoveTestBuilder.getTestPlan(rule1,rule2,rule1Name,rule2Name,facts);
  int i=0;
  for (  List<TestOperation> test : testPlan) {
    runAddRemoveTest(test,additionalGlobals);
  }
}
