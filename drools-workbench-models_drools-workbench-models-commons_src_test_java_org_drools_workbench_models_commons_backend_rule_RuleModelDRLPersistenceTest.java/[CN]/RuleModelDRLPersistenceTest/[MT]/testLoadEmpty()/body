{
  PackageDataModelOracle dmo=mock(PackageDataModelOracle.class);
  RuleModel m=RuleModelDRLPersistenceImpl.getInstance().unmarshal("",dmo);
  assertNotNull(m);
  m=RuleModelDRLPersistenceImpl.getInstance().unmarshal("",dmo);
  assertNotNull(m);
}
