{
  DroolsConsequenceAction action=(DroolsConsequenceAction)actionNode.getAction();
  xmlDump.append("    <action name=\"" + actionNode.getName() + "\" dialect=\""+ action.getDialect()+ "\">"+ action.getConsequence()+ "</action>"+ EOL);
}
