{
  return new StatefulKnowledgeSessionAdapter(delegate.newStatefulKnowledgeSession(conf == null ? null : ((KnowledgeSessionConfigurationAdapter)conf).getDelegate(),environment == null ? null : ((EnvironmentAdapter)environment).getDelegate()));
}
