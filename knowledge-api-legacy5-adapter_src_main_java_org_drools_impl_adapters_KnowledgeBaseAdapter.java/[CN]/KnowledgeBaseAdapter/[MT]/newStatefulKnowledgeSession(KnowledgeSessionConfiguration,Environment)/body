{
  return new StatefulKnowledgeSessionAdapter(delegate.newStatefulKnowledgeSession(((KnowledgeSessionConfigurationAdapter)conf).getDelegate(),environment == null ? null : ((EnvironmentAdapter)environment).getDelegate()));
}
