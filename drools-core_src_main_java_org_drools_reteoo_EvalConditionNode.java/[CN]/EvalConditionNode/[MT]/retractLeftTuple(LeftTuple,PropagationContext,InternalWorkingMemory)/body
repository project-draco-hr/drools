{
  final EvalMemory memory=(EvalMemory)workingMemory.getNodeMemory(this);
  final LeftTuple memTuple=memory.tupleMemory.remove(tuple);
  if (memTuple != null) {
    this.sink.propagateRetractLeftTuple(memTuple,context,workingMemory);
  }
}
