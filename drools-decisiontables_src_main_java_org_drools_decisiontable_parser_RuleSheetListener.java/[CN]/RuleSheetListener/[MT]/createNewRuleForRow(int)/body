{
  String name=_currentRulePrefix + "_" + row;
  Integer salience=null;
  if (_currentSequentialFlag) {
    salience=new Integer(Rule.calcSalience(row));
  }
  Rule rule=new Rule(name,salience,row);
  rule.setComment("From row number: " + (row + 1));
  return rule;
}
