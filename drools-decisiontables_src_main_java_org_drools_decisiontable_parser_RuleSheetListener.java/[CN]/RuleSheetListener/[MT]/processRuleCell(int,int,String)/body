{
  if (value.startsWith(RULE_TABLE_TAG)) {
    finishRuleTable();
    initRuleTable(row,column,value);
    return;
  }
  if (column < _ruleStartColumn) {
    return;
  }
  if (row == _ruleStartRow) {
    return;
  }
switch (row - _ruleStartRow) {
case ACTION_ROW:
    ActionType.addNewActionType(_actions,value,column,row);
  break;
case CODE_ROW:
codeRow(row,column,value);
break;
case LABEL_ROW:
labelRow(row,column,value);
break;
default :
nextRule(row,column,value);
break;
}
}
