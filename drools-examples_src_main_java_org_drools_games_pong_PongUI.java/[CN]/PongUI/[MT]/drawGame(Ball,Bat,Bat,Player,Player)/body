{
  clearMovingBall(ball);
  clearBat(bat1);
  clearBat(bat2);
  drawScore(p1,100);
  drawScore(p2,pconf.getTableWidth() - 120);
  drawTable();
  drawBall(ball);
  drawBat(bat1);
  drawBat(bat2);
  repaint();
}
