{
  Graphics g=getGraphics();
  int copyX=0 - (pconf.getTableWidth() - ball.getX() + 2);
  int copyY=ball.getY() - 2;
  g.copyArea(pconf.getTableWidth(),0,ball.getWidth() + 4,ball.getWidth() + 4,copyX,copyY);
}
