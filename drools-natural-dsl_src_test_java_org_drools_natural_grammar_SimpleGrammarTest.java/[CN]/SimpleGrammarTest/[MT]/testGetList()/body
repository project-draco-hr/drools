{
  SimpleGrammar g=new SimpleGrammar();
  g.addToDictionary("A","${all-left}.getSomething()");
  assertEquals("A",g.listNaturalItems()[0]);
  g.addToDictionary("B","XXX");
  assertEquals("B",g.listNaturalItems()[1]);
  g=new SimpleGrammar();
  assertEquals(0,g.listNaturalItems().length);
}
