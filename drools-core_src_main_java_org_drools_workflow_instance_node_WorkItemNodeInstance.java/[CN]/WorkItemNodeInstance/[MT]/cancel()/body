{
  if (workItemId != -1) {
    ((InternalWorkItemManager)((InternalProcessInstance)getProcessInstance()).getWorkingMemory().getWorkItemManager()).internalAbortWorkItem(workItemId);
  }
  super.cancel();
}
