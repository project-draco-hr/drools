{
  if (workItem == null && workItemId >= 0) {
    workItem=((InternalWorkItemManager)((InternalProcessInstance)getProcessInstance()).getWorkingMemory().getWorkItemManager()).getWorkItem(workItemId);
  }
  return workItem;
}
