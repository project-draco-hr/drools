{
  if (list == null) {
    list=new ArrayList();
  }
  for (  Dialect dialect : map.values()) {
    List results=dialect.getResults();
    if (results != null) {
      list.addAll(results);
    }
  }
  return list;
}
