{
  for (  ProtobufMessages.Activation _activation : _list) {
    context.filter.getActivationsMap().put(PersisterHelper.createActivationKey(_activation.getPackageName(),_activation.getRuleName(),_activation.getTuple()),_activation);
  }
}
