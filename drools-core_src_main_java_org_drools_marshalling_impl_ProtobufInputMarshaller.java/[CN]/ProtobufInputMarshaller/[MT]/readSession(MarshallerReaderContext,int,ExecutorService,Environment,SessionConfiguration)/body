{
  ProtobufMessages.KnowledgeSession _session=loadAndParseSession(context);
  System.out.println("input");
  System.out.println(_session.toString());
  ReteooStatefulSession session=createAndInitializeSession(context,id,executor,environment,config,_session);
  return readSession(_session,session,(DefaultAgenda)session.getAgenda(),context);
}
