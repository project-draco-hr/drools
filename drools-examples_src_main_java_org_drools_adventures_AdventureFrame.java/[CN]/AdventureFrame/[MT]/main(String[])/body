{
  EventQueue.invokeLater(new Runnable(){
    public void run(){
      try {
        AdventureFrame frame=new AdventureFrame();
        frame.setVisible(true);
      }
 catch (      Exception e) {
        e.printStackTrace();
      }
    }
  }
);
}
