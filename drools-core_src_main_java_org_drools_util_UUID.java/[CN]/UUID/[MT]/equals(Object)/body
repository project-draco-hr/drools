{
  if (!(o instanceof UUID)) {
    return false;
  }
  final byte[] otherId=((UUID)o).mId;
  final byte[] thisId=this.mId;
  for (int i=0; i < 16; ++i) {
    if (otherId[i] != thisId[i]) {
      return false;
    }
  }
  return true;
}
