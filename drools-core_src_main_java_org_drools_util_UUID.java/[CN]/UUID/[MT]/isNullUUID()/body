{
  if (this == UUID.sNullUUID) {
    return true;
  }
  final byte[] data=this.mId;
  int i=this.mId.length;
  final byte zero=(byte)0;
  while (--i >= 0) {
    if (data[i] != zero) {
      return false;
    }
  }
  return true;
}
