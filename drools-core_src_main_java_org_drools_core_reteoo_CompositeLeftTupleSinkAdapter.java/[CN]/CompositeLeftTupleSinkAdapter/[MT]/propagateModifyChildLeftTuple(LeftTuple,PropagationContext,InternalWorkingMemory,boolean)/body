{
  for (LeftTuple childLeftTuple=leftTuple.getFirstChild(); childLeftTuple != null; childLeftTuple=childLeftTuple.getHandleNext()) {
    childLeftTuple.getTupleSink().modifyLeftTuple(childLeftTuple,context,workingMemory);
  }
}
