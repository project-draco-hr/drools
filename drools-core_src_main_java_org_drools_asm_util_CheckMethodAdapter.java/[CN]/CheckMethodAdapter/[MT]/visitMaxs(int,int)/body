{
  checkStartCode();
  checkEndCode();
  this.endCode=true;
  checkUnsignedShort(maxStack,"Invalid min stack");
  checkUnsignedShort(maxLocals,"Invalid min locals");
  this.mv.visitMaxs(maxStack,maxLocals);
}
