{
  if (object == this) {
    return true;
  }
  return object instanceof LeftInputAdapterNode && this.hashCode() == object.hashCode() && this.sinkMask.equals(((LeftInputAdapterNode)object).sinkMask);
}
