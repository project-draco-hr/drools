{
  double angle1=get_angle(basepoint,p1);
  double angle2=get_angle(basepoint,p2);
  double temp=angle1 - angle2;
  if (temp < 0.0)   temp=-temp;
  if (temp > Math.PI)   temp=2 * Math.PI - temp;
  if (temp < 0.0)   return (-temp);
  return (temp);
}
