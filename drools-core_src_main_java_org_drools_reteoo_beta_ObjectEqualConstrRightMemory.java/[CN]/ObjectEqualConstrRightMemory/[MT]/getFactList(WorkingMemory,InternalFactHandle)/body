{
  final Object select=this.extractor.getValue(handle.getObject());
  MultiLinkedList list=(MultiLinkedList)this.memoryMap.get(select);
  if (list == null) {
    list=new KeyMultiLinkedList(select);
    this.memoryMap.put(select,list);
  }
  return list;
}
