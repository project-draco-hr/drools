{
  super.setUp();
  sessionBuilder=new ExampleRuleEngineFacade();
  sessionBuilder.addRuleExecutionSet(bindUri,StatelessRuleSessionTest.class.getResourceAsStream(bindUri));
  this.statelessSession=sessionBuilder.getStatelessRuleSession(bindUri);
}
