{
  SlidingTimeWindowContext queue=(SlidingTimeWindowContext)context;
  queue.queue.add(rightTuple);
  if (queue.queue.peek() == rightTuple) {
    updateNextExpiration(rightTuple,workingMemory,queue);
  }
  return true;
}
