{
  if (this.expiringTuple != rightTuple) {
    PriorityQueue<RightTuple> queue=(PriorityQueue<RightTuple>)context;
    if (queue.peek() == rightTuple) {
      queue.poll();
      updateNextExpiration(queue.peek(),workingMemory,queue);
    }
 else {
      queue.remove(rightTuple);
    }
  }
}
