{
  SessionClock clock=((TemporalSession<SessionClock>)workingMemory).getSessionClock();
  if (rightTuple != null) {
    clock.schedule(this,context,((EventFactHandle)rightTuple.getFactHandle()).getStartTimestamp() + this.size);
  }
 else {
    clock.unschedule(this);
  }
}
