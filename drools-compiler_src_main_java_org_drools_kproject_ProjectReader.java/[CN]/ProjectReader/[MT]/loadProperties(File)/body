{
  Properties props=new Properties();
  if (file.exists()) {
    InputStream is=null;
    try {
      is=file.getContents();
      props.load(is);
    }
  finally {
      if (is != null) {
        is.close();
      }
    }
  }
  return props;
}
