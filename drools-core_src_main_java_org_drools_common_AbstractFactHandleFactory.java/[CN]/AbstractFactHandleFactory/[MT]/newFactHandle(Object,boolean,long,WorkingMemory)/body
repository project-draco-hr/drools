{
  if (!this.factHandlePool.isEmpty()) {
    return newFactHandle(this.factHandlePool.pop(),object,isEvent,duration,workingMemory);
  }
  return newFactHandle(this.id++,object,isEvent,duration,workingMemory);
}
