{
  Evaluator evaluator=EvaluatorFactory.getEvaluator(valueType,evaluatorString);
  if (evaluator == null) {
    this.results.add(new BuilderResult(descr,null,"Unable to determine the Evaluator for  '" + valueType + "' and '"+ evaluatorString+ "'"));
  }
  return evaluator;
}
