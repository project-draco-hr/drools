{
  List usedDeclarations=null;
  try {
    usedDeclarations=this.analyzer.analyze(text,this.declarations.keySet());
  }
 catch (  Exception e) {
    this.results.add(new BuilderResult(descr,null,"Unable to determine the used declarations"));
  }
  return usedDeclarations;
}
