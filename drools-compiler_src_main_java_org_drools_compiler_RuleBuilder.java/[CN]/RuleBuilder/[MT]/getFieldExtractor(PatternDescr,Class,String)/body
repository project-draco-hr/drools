{
  FieldExtractor extractor=null;
  try {
    extractor=new ClassFieldExtractor(clazz,fieldName);
  }
 catch (  RuntimeDroolsException e) {
    this.results.add(new BuilderResult(descr,e,"Unable to create Field Extractor for '" + fieldName) + "'");
  }
  return extractor;
}
