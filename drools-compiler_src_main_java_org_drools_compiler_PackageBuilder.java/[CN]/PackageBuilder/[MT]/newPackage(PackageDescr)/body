{
  this.pkg=new Package(packageDescr.getName(),this.configuration.getClassLoader());
  this.typeResolver.addImport(this.pkg.getName() + ".*");
  this.dialectRegistry.initAll(this);
  mergePackage(packageDescr);
}
