{
  final FunctionBuilder buidler=new FunctionBuilder();
  this.pkg.addFunction(functionDescr.getName());
  addClassCompileTask(this.pkg.getName() + "." + ucFirst(functionDescr.getName()),buidler.build(this.pkg,functionDescr,getFunctionFixer(),lineMappings),this.src,new FunctionErrorHandler(functionDescr,"Function Compilation error"));
}
