{
  src.add(className.replace('.','/') + ".java",text.getBytes());
  final CompilationResult result=this.compiler.compile(new String[]{className},src,dst,this.pkg.getPackageCompilationData().getClassLoader());
  return result;
}
