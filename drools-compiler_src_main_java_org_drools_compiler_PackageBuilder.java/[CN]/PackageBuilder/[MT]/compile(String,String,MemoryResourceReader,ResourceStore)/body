{
  src.add(className.replace('.','/') + ".java",text.getBytes());
  CompilationResult result=compiler.compile(new String[]{className},src,dst,pkg.getPackageCompilationData().getClassLoader());
  return result;
}
