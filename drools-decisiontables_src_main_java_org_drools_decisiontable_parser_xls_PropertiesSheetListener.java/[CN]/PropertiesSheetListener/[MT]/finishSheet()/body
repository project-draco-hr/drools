{
  for (Iterator iter=_rowProperties.keySet().iterator(); iter.hasNext(); ) {
    String[] keyValue=(String[])_rowProperties.get(iter.next());
    _properties.setProperty(keyValue[0],keyValue[1]);
  }
}
