{
  List<String[]> pcList=getPropertyCell(key);
  if (pcList == null) {
    return null;
  }
  List<String> r=new ArrayList<String>();
  for (  String[] pc : pcList) {
    r.add(pc[index]);
  }
  return r;
}
