{
  if (this == object) {
    return true;
  }
  return object instanceof AlphaNode && this.hashCode() == object.hashCode() && (constraint instanceof MvelConstraint ? ((MvelConstraint)constraint).equals(((AlphaNode)object).constraint,getKnowledgeBase()) : constraint.equals(((AlphaNode)object).constraint));
}
