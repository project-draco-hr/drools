{
  int[][] array=new int[][]{{0,0},{0,1},{1,0},{1,1}};
  int[][] clone=(int[][])ShadowProxyUtils.cloneObject(array);
  assertTrue(ArrayUtils.deepEquals(array,clone));
  assertNotSame(array,clone);
}
