{
  Map map=new TreeMap();
  map.put("a","a");
  map.put("b","b");
  Map unmod=Collections.unmodifiableMap(map);
  Map clone=(Map)ShadowProxyUtils.cloneObject(unmod);
  assertEquals(unmod,clone);
  assertSame(unmod,clone);
}
