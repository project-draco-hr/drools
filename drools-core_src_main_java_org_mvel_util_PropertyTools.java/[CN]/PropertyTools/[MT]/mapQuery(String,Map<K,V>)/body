{
  Pattern qPattern=wildcardToRegex(wildcard);
  if (map == null)   return null;
  Map<K,V> newMap=new HashMap<K,V>(map.size() * 2);
  for (  K name : map.keySet()) {
    if (qPattern.matcher(valueOf(name)).find())     newMap.put(name,map.get(name));
  }
  return newMap;
}
