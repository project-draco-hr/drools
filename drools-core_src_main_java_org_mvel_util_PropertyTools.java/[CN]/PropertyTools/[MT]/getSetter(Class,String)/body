{
  String setter=ReflectionUtil.getSetter(property);
  for (  Method meth : clazz.getDeclaredMethods()) {
    if ((meth.getModifiers() & PUBLIC) == 0 && meth.getParameterTypes().length != 0)     continue;
    if (setter.equals(meth.getName())) {
      return meth;
    }
  }
  return null;
}
