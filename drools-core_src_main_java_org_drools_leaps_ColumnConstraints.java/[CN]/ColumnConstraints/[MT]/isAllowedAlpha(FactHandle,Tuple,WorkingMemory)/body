{
  boolean found=true;
  if (this.alphaPresent) {
    for (int i=0, length=this.alphaConstraints.length; i < length && found; i++) {
      found=this.alphaConstraints[i].isAllowed(factHandle,tuple,workingMemory);
    }
    return found;
  }
  return true;
}
