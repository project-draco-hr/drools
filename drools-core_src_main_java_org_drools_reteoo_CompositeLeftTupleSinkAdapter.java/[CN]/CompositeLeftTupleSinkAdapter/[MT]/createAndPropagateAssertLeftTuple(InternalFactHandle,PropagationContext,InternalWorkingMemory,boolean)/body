{
  for (LeftTupleSinkNode sink=this.sinks.getFirst(); sink != null; sink=sink.getNextLeftTupleSinkNode()) {
    sink.assertLeftTuple(new LeftTuple(factHandle,sink,leftTupleMemoryEnabled),context,workingMemory);
  }
}
