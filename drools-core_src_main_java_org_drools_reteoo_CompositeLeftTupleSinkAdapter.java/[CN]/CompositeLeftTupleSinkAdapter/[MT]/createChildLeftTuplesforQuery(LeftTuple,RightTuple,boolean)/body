{
  for (LeftTupleSinkNode sink=this.sinks.getLast(); sink != null; sink=sink.getPreviousLeftTupleSinkNode()) {
    LeftTuple child=new LeftTuple(leftTuple,rightTuple.getFactHandle(),sink,leftTupleMemoryEnabled);
  }
}
