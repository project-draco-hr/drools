{
  if (this.innerMemory != null) {
    this.innerMemory.remove(workingMemory,(MultiLinkedListNodeWrapper)tuple.getChild());
  }
  LinkedList list=tuple.getLinkedList();
  list.remove(tuple);
  this.size--;
  if (list.isEmpty()) {
    this.removeMemoryEntry(list);
  }
}
