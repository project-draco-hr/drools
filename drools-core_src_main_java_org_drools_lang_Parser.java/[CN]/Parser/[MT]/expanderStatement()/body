{
  String line=laDiscard();
  if (line == null) {
    return false;
  }
  Matcher matcher=EXPANDER_STATEMENT.matcher(line);
  if (matcher.matches()) {
    consumeDiscard();
    expanderName=matcher.group(1);
    return true;
  }
  return false;
}
