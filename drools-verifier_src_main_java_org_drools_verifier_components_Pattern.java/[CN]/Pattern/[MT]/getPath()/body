{
  String parentPath=getParentPath();
  if (parentPath == null) {
    return String.format("%s.pattern[%s]",getRulePath(),getOrderNumber());
  }
 else {
    return String.format("%s.pattern[%s]",getParentPath(),getOrderNumber());
  }
}
