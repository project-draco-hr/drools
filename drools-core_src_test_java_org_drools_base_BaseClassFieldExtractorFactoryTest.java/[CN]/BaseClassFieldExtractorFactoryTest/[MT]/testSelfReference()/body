{
  final FieldExtractor ex=ClassFieldExtractorCache.getExtractor(BeanInherit.class,"this",getClass().getClassLoader());
  final TestBean bean=new TestBean();
  assertEquals(bean,ex.getValue(bean));
}
