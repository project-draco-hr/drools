{
  final FieldExtractor ex=ClassFieldExtractorCache.getExtractor(TestInterface.class,"something",getClass().getClassLoader());
  assertEquals(0,ex.getIndex());
  assertEquals("foo",ex.getValue(new TestInterfaceImpl()));
}
