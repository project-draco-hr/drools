{
  final FieldExtractor ex=ClassFieldExtractorCache.getExtractor(TestAbstract.class,"something",getClass().getClassLoader());
  assertEquals(0,ex.getIndex());
  assertEquals("foo",ex.getValue(new TestAbstractImpl()));
}
