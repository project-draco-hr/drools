{
  lock.lock();
  try {
    if (!tasks.isEmpty()) {
      try {
        empty.await();
      }
 catch (      InterruptedException e) {
        Thread.currentThread().interrupt();
      }
    }
 else {
    }
  }
  finally {
    lock.unlock();
  }
}
