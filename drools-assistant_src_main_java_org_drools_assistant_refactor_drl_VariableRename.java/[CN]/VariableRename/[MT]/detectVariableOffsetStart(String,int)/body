{
  for (int position=offset; position > 0; position--) {
    if (line.charAt(position) == ',' || line.charAt(position) == '(')     return position + 1;
    if (line.charAt(position) == ':' || line.charAt(position) == '.')     return -1;
  }
  return 0;
}
