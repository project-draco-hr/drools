{
  for (int position=0; position < varname.length(); position++)   if (varname.charAt(position) == '$')   return varname.substring(0,position) + "\\$" + varname.substring(position + 1,varname.length());
  return varname;
}
