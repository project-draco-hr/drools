{
  String n=name + "(";
  for (Iterator<String> iterator=params.iterator(); iterator.hasNext(); ) {
    n+=iterator.next();
    if (iterator.hasNext()) {
      n+=",";
    }
  }
  n+=")";
  return n;
}
