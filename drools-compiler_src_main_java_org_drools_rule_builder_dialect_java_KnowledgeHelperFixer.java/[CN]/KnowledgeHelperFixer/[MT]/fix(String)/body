{
  String result=fix(raw,UpdateReplacer.INSTANCE);
  result=fix(result,InsertReplacer.INSTANCE);
  result=fix(result,InsertLogicalReplacer.INSTANCE);
  result=fix(result,RetractReplacer.INSTANCE);
  return result;
}
