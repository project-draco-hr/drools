{
  StringBuffer buf=new StringBuffer();
  buf.append("rule \"" + model.name + "\"\n");
  this.marshalAttributes(buf,model);
  buf.append("\twhen\n");
  this.marshalLHS(buf,model);
  buf.append("\tthen\n");
  this.marshalRHS(buf,model);
  buf.append("end\n");
  return buf.toString();
}
