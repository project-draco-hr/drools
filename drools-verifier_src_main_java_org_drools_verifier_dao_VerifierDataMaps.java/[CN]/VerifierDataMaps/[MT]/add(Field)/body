{
  ObjectType objectType=classesById.get(Integer.valueOf(field.getClassId()));
  fieldsByClassAndFieldName.put(objectType.getName() + "." + field.getName(),field);
  fieldsById.put(field.getId(),field);
  fieldsByClassId.put(field.getClassId(),field);
}
