{
synchronized (putLock) {
synchronized (takeLock) {
      s.defaultWriteObject();
      for (Node p=head.next; p != null; p=p.next)       s.writeObject(p.item);
      s.writeObject(null);
    }
  }
}
