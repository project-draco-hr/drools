{
  s.defaultReadObject();
synchronized (this) {
    count=0;
  }
  last=head=new Node(null);
  for (; ; ) {
    Object item=(Object)s.readObject();
    if (item == null)     break;
    add(item);
  }
}
