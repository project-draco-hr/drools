{
  String result=null;
  try {
    Integer min=new Integer(applicationProperties.getProperty("range.min"));
    Integer max=new Integer(applicationProperties.getProperty("range.max"));
    if (operator.indexOf("<") != -1)     max=new Integer(value);
 else     if (operator.indexOf(">") != -1)     min=new Integer(value);
    result=min.toString();
    for (int i=min.intValue() + 1; i < max.intValue(); i++)     result=result + "," + i;
  }
 catch (  PropertyNotFoundException e) {
  }
  return result;
}
