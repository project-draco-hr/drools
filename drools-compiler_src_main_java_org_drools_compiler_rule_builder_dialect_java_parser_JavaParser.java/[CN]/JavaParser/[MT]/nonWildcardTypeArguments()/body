{
  int nonWildcardTypeArguments_StartIndex=input.index();
  try {
    if (state.backtracking > 0 && alreadyParsedRule(input,136)) {
      return;
    }
{
      match(input,48,FOLLOW_48_in_nonWildcardTypeArguments6218);
      if (state.failed)       return;
      pushFollow(FOLLOW_typeList_in_nonWildcardTypeArguments6220);
      typeList();
      state._fsp--;
      if (state.failed)       return;
      match(input,51,FOLLOW_51_in_nonWildcardTypeArguments6222);
      if (state.failed)       return;
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
    if (state.backtracking > 0) {
      memoize(input,136,nonWildcardTypeArguments_StartIndex);
    }
  }
}
