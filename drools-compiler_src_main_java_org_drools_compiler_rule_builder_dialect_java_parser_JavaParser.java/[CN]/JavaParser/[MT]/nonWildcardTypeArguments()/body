{
  int nonWildcardTypeArguments_StartIndex=input.index();
  try {
    if (state.backtracking > 0 && alreadyParsedRule(input,135)) {
      return;
    }
{
      match(input,48,FOLLOW_48_in_nonWildcardTypeArguments6163);
      if (state.failed)       return;
      pushFollow(FOLLOW_typeList_in_nonWildcardTypeArguments6165);
      typeList();
      state._fsp--;
      if (state.failed)       return;
      match(input,51,FOLLOW_51_in_nonWildcardTypeArguments6167);
      if (state.failed)       return;
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
    if (state.backtracking > 0) {
      memoize(input,135,nonWildcardTypeArguments_StartIndex);
    }
  }
}
