{
  if (list.size() > 1) {
    return "<span>" + list.get(0) + "...</span>";
  }
 else   if (list.size() == 1) {
    return "<span>" + list.get(0) + "</span>";
  }
 else {
    return "<span></span>";
  }
}
