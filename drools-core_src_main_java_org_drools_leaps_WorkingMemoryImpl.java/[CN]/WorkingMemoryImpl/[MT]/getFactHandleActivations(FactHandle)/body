{
  Iterator ret=null;
  RetractAssembly assembly=(RetractAssembly)this.retracts.get(((FactHandleImpl)factHandle).getId());
  if (assembly != null) {
    ret=assembly.getPostedActivations();
  }
  return ret;
}
