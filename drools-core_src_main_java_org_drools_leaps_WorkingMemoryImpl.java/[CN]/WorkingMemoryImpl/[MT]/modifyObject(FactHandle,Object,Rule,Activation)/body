{
  PropagationContext propagationContext=new PropagationContextImpl(++this.propagationIdCounter,PropagationContext.MODIFICATION,rule,activation);
  this.retractObject(handle);
  this.assertObject(object);
  this.workingMemoryEventSupport.fireObjectModified(propagationContext,handle,((FactHandleImpl)handle).getObject(),object);
}
