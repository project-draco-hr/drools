{
  for (Iterator alphas=constraints.getAlpha(); alphas.hasNext(); ) {
    if (!((LiteralConstraint)alphas.next()).isAllowed(factHandle.getObject())) {
      return false;
    }
  }
  return constraints.getBeta().isAllowed(factHandle.getObject(),factHandle,token,this);
}
