{
  for (Iterator alphas=constraints.getAlpha(); alphas.hasNext(); ) {
    if (!((LiteralConstraint)alphas.next()).isAllowed(factHandle,token,this)) {
      return false;
    }
  }
  return constraints.getBeta().isAllowed(factHandle,token,this);
}
