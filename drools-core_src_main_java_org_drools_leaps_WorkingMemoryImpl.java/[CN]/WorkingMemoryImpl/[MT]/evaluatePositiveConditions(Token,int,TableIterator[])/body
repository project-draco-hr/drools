{
  FactHandleImpl factHandle=(FactHandleImpl)iterators[index].current();
  return checkConstraints(token,token.getCurrentRuleHandle().getLeapsRule().getColumnConstraintsAtPosition(index),factHandle);
}
