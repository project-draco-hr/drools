{
  if (this.entry == null) {
    row++;
    if (row == length) {
      return null;
    }
    this.entry=this.table[row];
    if (this.entry == null) {
      this.entry=next();
    }
  }
 else {
    this.entry=this.entry.getNext();
    if (this.entry == null) {
      this.entry=next();
    }
  }
  return this.entry;
}
