{
  String input="( fnord( \"cheese\" ) )";
  createParser(new ANTLRStringStream(input));
  String returnData=parser.chunk(DRLLexer.LEFT_PAREN,DRLLexer.RIGHT_PAREN);
  assertEquals("fnord( \"cheese\" )",returnData);
}
