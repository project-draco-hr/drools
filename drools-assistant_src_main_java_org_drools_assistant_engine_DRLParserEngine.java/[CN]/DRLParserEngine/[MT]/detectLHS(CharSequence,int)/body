{
  pattern=Pattern.compile(RULE_LHS_PATTERN);
  matcher=pattern.matcher(rule);
  if (matcher.find())   return detectLines(matcher.group(),matcher.start() + ruleOffset,DRLContentTypeEnum.RULE_LHS_LINE);
  return null;
}
