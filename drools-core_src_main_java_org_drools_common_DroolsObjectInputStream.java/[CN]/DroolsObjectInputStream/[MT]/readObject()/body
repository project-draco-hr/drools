{
  byte type=readRecordType();
switch (type) {
case RT_NULL:
    return readNull();
case RT_EMPTY_SET:
  return readEmptySet();
case RT_EMPTY_LIST:
return readEmptyList();
case RT_EMPTY_MAP:
return readEmptyMap();
default :
return readObject(type);
}
}
