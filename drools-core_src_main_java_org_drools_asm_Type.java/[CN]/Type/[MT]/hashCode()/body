{
  int hc=13 * this.sort;
  if (this.sort == Type.OBJECT || this.sort == Type.ARRAY) {
    for (int i=this.off, end=i + this.len; i < end; i++) {
      hc=17 * (hc + this.buf[i]);
    }
  }
  return hc;
}
