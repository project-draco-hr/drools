{
  try {
    PackageDescrVisitor ruleFlattener=new PackageDescrVisitor();
    ruleFlattener.addPackageDescrToData(descr,jars,result.getVerifierData());
  }
 catch (  Throwable t) {
    t.printStackTrace();
  }
}
