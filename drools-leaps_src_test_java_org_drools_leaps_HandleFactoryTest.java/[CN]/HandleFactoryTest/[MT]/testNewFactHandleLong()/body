{
  this.factory.getNextId();
  this.factory.getNextId();
  final long nextId=this.factory.getNextId() + 1;
  assertEquals(nextId,((DefaultFactHandle)this.factory.newFactHandle(984393L,new Object())).getId());
}
