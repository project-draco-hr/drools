{
  Accumulate clone=new Accumulate((RuleConditionElement)this.source.clone(),this.requiredDeclarations,this.innerDeclarations,this.accumulators,this.multiFunction);
  if (this.cloned == Collections.EMPTY_LIST) {
    this.cloned=new ArrayList<Accumulate>(1);
  }
  this.cloned.add(clone);
  return clone;
}
