{
  int start=cursor - 10;
  int end=(cursor + 20);
  if (start < 0) {
    start=0;
  }
  if (end > length) {
    end=length - 1;
  }
  return "'" + copyValueOf(expressionArray,start,end - start) + "'";
}
