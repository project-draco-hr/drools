{
  InputStream is;
  if (reader instanceof InputStreamReader) {
    is=new ReaderInputStream(reader,((InputStreamReader)reader).getEncoding());
  }
 else {
    is=new ReaderInputStream(reader);
  }
  return compileStream(is,configuration);
}
