{
  return new ComponentInfo("RuleBases","Here you select packages to assemble rulebases. Rulebases are the executable unit that the " + "runtime engine uses."){
    public JBRMSFeature createInstance(){
      return new RuleBases();
    }
    public Image getImage(){
      return new Image("images/config.png");
    }
  }
;
}
