{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  if (!super.equals(o)) {
    return false;
  }
  LookCommand that=(LookCommand)o;
  if (!character.equals(that.character)) {
    return false;
  }
  return true;
}
