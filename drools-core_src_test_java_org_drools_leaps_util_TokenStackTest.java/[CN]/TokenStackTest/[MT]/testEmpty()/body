{
  final TokenStack stack=new TokenStack();
  assertTrue(stack.empty());
  final Token token=new Token(null,new FactHandleImpl(3,new Object()),null);
  stack.push(token);
  assertFalse(stack.empty());
}
