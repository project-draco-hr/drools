{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ExitEvent exitEvent=(ExitEvent)o;
  if (!character.equals(exitEvent.character)) {
    return false;
  }
  if (!room.equals(exitEvent.room)) {
    return false;
  }
  return true;
}
