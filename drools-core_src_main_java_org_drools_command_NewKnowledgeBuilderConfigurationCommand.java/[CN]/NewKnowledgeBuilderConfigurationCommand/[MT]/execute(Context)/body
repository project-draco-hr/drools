{
  System.out.println("default Context.get -> " + context.getContextManager().getDefaultContext().get(kbuilderConfId));
  context.getContextManager().getDefaultContext().set(kbuilderConfId,KnowledgeBuilderFactory.newKnowledgeBuilderConfiguration());
  return null;
}
