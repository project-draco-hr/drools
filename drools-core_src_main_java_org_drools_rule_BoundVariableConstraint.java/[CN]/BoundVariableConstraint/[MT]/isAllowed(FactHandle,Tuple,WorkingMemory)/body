{
  Object left=workingMemory.getObject(tuple.get(this.declaration));
  Object right=workingMemory.getObject(handle);
  if (left == right) {
    return false;
  }
 else {
    return evaluator.evaluate(this.fieldExtractor.getValue(right),declaration.getValue(left));
  }
}
