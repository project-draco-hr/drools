{
  final Object select=tuple.get(this.column).getObject();
  this.selectedObject=this.memoryMap.get(select);
  if (this.innerMemory != null) {
    this.innerMemory.selectPossibleMatches(workingMemory,tuple);
  }
}
