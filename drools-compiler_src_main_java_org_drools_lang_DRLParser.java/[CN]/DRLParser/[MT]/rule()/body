{
  RuleDescr rule=null;
  Token loc=null;
  String ruleName=null;
  rule=null;
  String consequence="";
  AndDescr lhs=null;
  try {
{
      loc=(Token)input.LT(1);
      match(input,RULE,FOLLOW_RULE_in_rule763);
      if (failed)       return rule;
      pushFollow(FOLLOW_name_in_rule767);
      ruleName=name();
      _fsp--;
      if (failed)       return rule;
      if (backtracking == 0) {
        debug("start rule: " + ruleName);
        rule=new RuleDescr(ruleName,null);
        rule.setLocation(offset(loc.getLine()),loc.getCharPositionInLine());
        rule.setStartCharacter(((CommonToken)loc).getStartIndex());
      }
      pushFollow(FOLLOW_rule_attributes_in_rule776);
      rule_attributes(rule);
      _fsp--;
      if (failed)       return rule;
      int alt14=2;
      int LA14_0=input.LA(1);
      if ((LA14_0 == WHEN)) {
        alt14=1;
      }
switch (alt14) {
case 1:
{
          loc=(Token)input.LT(1);
          match(input,WHEN,FOLLOW_WHEN_in_rule785);
          if (failed)           return rule;
          int alt13=2;
          int LA13_0=input.LA(1);
          if ((LA13_0 == 65)) {
            alt13=1;
          }
switch (alt13) {
case 1:
{
              match(input,65,FOLLOW_65_in_rule787);
              if (failed)               return rule;
            }
          break;
      }
      if (backtracking == 0) {
        lhs=new AndDescr();
        rule.setLhs(lhs);
        lhs.setLocation(offset(loc.getLine()),loc.getCharPositionInLine());
      }
{
        pushFollow(FOLLOW_normal_lhs_block_in_rule805);
        normal_lhs_block(lhs);
        _fsp--;
        if (failed)         return rule;
      }
    }
  break;
}
pushFollow(FOLLOW_rhs_chunk_in_rule826);
rhs_chunk(rule);
_fsp--;
if (failed) return rule;
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return rule;
}
