{
  int index=input.index();
  while (index >= 0) {
    int type=input.get(index).getType();
switch (type) {
case DRLLexer.EOF:
      index--;
    break;
case DRLLexer.WS:
  return true;
default :
return false;
}
}
return false;
}
