{
  RestrictionDescr rd=null;
  String op=null;
  RestrictionConnectiveDescr group=null;
  try {
{
      int alt48=2;
      int LA48_0=input.LA(1);
      if ((LA48_0 == IN)) {
        alt48=1;
      }
 else       if ((LA48_0 == NOT)) {
        alt48=2;
      }
 else {
        if (backtracking > 0) {
          failed=true;
          return;
        }
        NoViableAltException nvae=new NoViableAltException("1199:3: ( IN | NOT IN )",48,0,input);
        throw nvae;
      }
switch (alt48) {
case 1:
{
          match(input,IN,FOLLOW_IN_in_compound_operator3101);
          if (failed)           return;
          if (backtracking == 0) {
            op="==";
            group=new RestrictionConnectiveDescr(RestrictionConnectiveDescr.OR);
            base.addRestriction(group);
            location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_ARGUMENT);
            location.setProperty(Location.LOCATION_PROPERTY_OPERATOR,"in");
          }
        }
      break;
case 2:
{
      match(input,NOT,FOLLOW_NOT_in_compound_operator3113);
      if (failed)       return;
      match(input,IN,FOLLOW_IN_in_compound_operator3115);
      if (failed)       return;
      if (backtracking == 0) {
        op="!=";
        group=new RestrictionConnectiveDescr(RestrictionConnectiveDescr.AND);
        base.addRestriction(group);
        location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_ARGUMENT);
        location.setProperty(Location.LOCATION_PROPERTY_OPERATOR,"in");
      }
    }
  break;
}
match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_compound_operator3130);
if (failed) return;
pushFollow(FOLLOW_expression_value_in_compound_operator3134);
rd=expression_value(op);
_fsp--;
if (failed) return;
if (backtracking == 0) {
if (rd != null) {
  group.addRestriction(rd);
}
}
loop49: do {
int alt49=2;
int LA49_0=input.LA(1);
if ((LA49_0 == COMMA)) {
  alt49=1;
}
switch (alt49) {
case 1:
{
    match(input,COMMA,FOLLOW_COMMA_in_compound_operator3146);
    if (failed)     return;
    pushFollow(FOLLOW_expression_value_in_compound_operator3150);
    rd=expression_value(op);
    _fsp--;
    if (failed)     return;
    if (backtracking == 0) {
      if (rd != null) {
        group.addRestriction(rd);
      }
    }
  }
break;
default :
break loop49;
}
}
 while (true);
match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_compound_operator3166);
if (failed) return;
if (backtracking == 0) {
location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_END);
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
