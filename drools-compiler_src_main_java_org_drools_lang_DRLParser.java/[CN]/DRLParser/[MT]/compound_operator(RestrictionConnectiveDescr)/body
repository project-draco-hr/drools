{
  RestrictionDescr rd=null;
  String op=null;
  RestrictionConnectiveDescr group=null;
  try {
{
      int alt73=2;
      int LA73_0=input.LA(1);
      if ((LA73_0 == IN)) {
        alt73=1;
      }
 else       if ((LA73_0 == NOT)) {
        alt73=2;
      }
 else {
        if (backtracking > 0) {
          failed=true;
          return;
        }
        NoViableAltException nvae=new NoViableAltException("1446:3: ( IN | NOT IN )",73,0,input);
        throw nvae;
      }
switch (alt73) {
case 1:
{
          match(input,IN,FOLLOW_IN_in_compound_operator4039);
          if (failed)           return;
          if (backtracking == 0) {
            op="==";
            group=new RestrictionConnectiveDescr(RestrictionConnectiveDescr.OR);
            base.addRestriction(group);
            location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_ARGUMENT);
            location.setProperty(Location.LOCATION_PROPERTY_OPERATOR,"in");
          }
        }
      break;
case 2:
{
      match(input,NOT,FOLLOW_NOT_in_compound_operator4051);
      if (failed)       return;
      match(input,IN,FOLLOW_IN_in_compound_operator4053);
      if (failed)       return;
      if (backtracking == 0) {
        op="!=";
        group=new RestrictionConnectiveDescr(RestrictionConnectiveDescr.AND);
        base.addRestriction(group);
        location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_ARGUMENT);
        location.setProperty(Location.LOCATION_PROPERTY_OPERATOR,"in");
      }
    }
  break;
}
match(input,LEFT_PAREN,FOLLOW_LEFT_PAREN_in_compound_operator4068);
if (failed) return;
pushFollow(FOLLOW_expression_value_in_compound_operator4072);
rd=expression_value(group,op);
_fsp--;
if (failed) return;
loop74: do {
int alt74=2;
int LA74_0=input.LA(1);
if ((LA74_0 == COMMA)) {
  alt74=1;
}
switch (alt74) {
case 1:
{
    match(input,COMMA,FOLLOW_COMMA_in_compound_operator4079);
    if (failed)     return;
    pushFollow(FOLLOW_expression_value_in_compound_operator4083);
    rd=expression_value(group,op);
    _fsp--;
    if (failed)     return;
  }
break;
default :
break loop74;
}
}
 while (true);
match(input,RIGHT_PAREN,FOLLOW_RIGHT_PAREN_in_compound_operator4092);
if (failed) return;
if (backtracking == 0) {
location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_END);
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
