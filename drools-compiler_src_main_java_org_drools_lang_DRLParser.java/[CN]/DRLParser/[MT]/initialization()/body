{
  String expr=null;
  paren_chunk_return val=null;
  try {
{
      match(input,EQUALS,FOLLOW_EQUALS_in_initialization900);
      if (failed)       return expr;
      pushFollow(FOLLOW_paren_chunk_in_initialization904);
      val=paren_chunk();
      _fsp--;
      if (failed)       return expr;
      if (backtracking == 0) {
        expr=getString(input.toString(val.start,val.stop)).trim();
      }
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return expr;
}
