{
  AndDescr and=new AndDescr();
  try {
{
      pushFollow(FOLLOW_unary_constr_in_and_constr3419);
      unary_constr(and);
      _fsp--;
      if (failed)       return;
      loop60:       do {
        int alt60=2;
        int LA60_0=input.LA(1);
        if ((LA60_0 == DOUBLE_AMPER)) {
          alt60=1;
        }
switch (alt60) {
case 1:
{
            match(input,DOUBLE_AMPER,FOLLOW_DOUBLE_AMPER_in_and_constr3427);
            if (failed)             return;
            if (backtracking == 0) {
              location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_START);
            }
            pushFollow(FOLLOW_unary_constr_in_and_constr3436);
            unary_constr(and);
            _fsp--;
            if (failed)             return;
          }
        break;
default :
      break loop60;
  }
}
 while (true);
if (backtracking == 0) {
  if (and.getDescrs().size() == 1) {
    base.addOrMerge((BaseDescr)and.getDescrs().get(0));
  }
 else   if (and.getDescrs().size() > 1) {
    base.addDescr(and);
  }
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
