{
  AndDescr and=new AndDescr();
  try {
{
      pushFollow(FOLLOW_unary_constr_in_and_constr3473);
      unary_constr(and);
      _fsp--;
      if (failed)       return;
      loop64:       do {
        int alt64=2;
        int LA64_0=input.LA(1);
        if ((LA64_0 == DOUBLE_AMPER)) {
          alt64=1;
        }
switch (alt64) {
case 1:
{
            match(input,DOUBLE_AMPER,FOLLOW_DOUBLE_AMPER_in_and_constr3481);
            if (failed)             return;
            if (backtracking == 0) {
              location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_START);
            }
            pushFollow(FOLLOW_unary_constr_in_and_constr3490);
            unary_constr(and);
            _fsp--;
            if (failed)             return;
          }
        break;
default :
      break loop64;
  }
}
 while (true);
if (backtracking == 0) {
  if (and.getDescrs().size() == 1) {
    base.addOrMerge((BaseDescr)and.getDescrs().get(0));
  }
 else   if (and.getDescrs().size() > 1) {
    base.addDescr(and);
  }
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
