{
{
    if (input.LA(1) == OR || input.LA(1) == 67) {
      input.consume();
      errorRecovery=false;
      failed=false;
    }
 else {
      if (backtracking > 0) {
        failed=true;
        return;
      }
      MismatchedSetException mse=new MismatchedSetException(null,input);
      recoverFromMismatchedSet(input,mse,FOLLOW_set_in_synpred422127);
      throw mse;
    }
    pushFollow(FOLLOW_fact_in_synpred422144);
    fact();
    _fsp--;
    if (failed)     return;
  }
}
