{
  String name=null;
  Token star=null;
  Token ID4=null;
  Token DOT5=null;
  identifier_return id=null;
  name=null;
  try {
{
      ID4=(Token)input.LT(1);
      match(input,ID,FOLLOW_ID_in_import_name405);
      if (failed)       return name;
      if (backtracking == 0) {
        name=ID4.getText();
        importDecl.setTarget(name);
        importDecl.setEndCharacter(((CommonToken)ID4).getStopIndex());
      }
      loop5:       do {
        int alt5=2;
        int LA5_0=input.LA(1);
        if ((LA5_0 == DOT)) {
          alt5=1;
        }
switch (alt5) {
case 1:
{
            DOT5=(Token)input.LT(1);
            match(input,DOT,FOLLOW_DOT_in_import_name417);
            if (failed)             return name;
            pushFollow(FOLLOW_identifier_in_import_name421);
            id=identifier();
            _fsp--;
            if (failed)             return name;
            if (backtracking == 0) {
              name=name + DOT5.getText() + input.toString(id.start,id.stop);
              importDecl.setTarget(name);
              importDecl.setEndCharacter(((CommonToken)((Token)id.start)).getStopIndex());
            }
          }
        break;
default :
      break loop5;
  }
}
 while (true);
int alt6=2;
int LA6_0=input.LA(1);
if ((LA6_0 == 75)) {
  alt6=1;
}
switch (alt6) {
case 1:
{
    star=(Token)input.LT(1);
    match(input,75,FOLLOW_75_in_import_name445);
    if (failed)     return name;
    if (backtracking == 0) {
      name=name + star.getText();
      importDecl.setTarget(name);
      importDecl.setEndCharacter(((CommonToken)star).getStopIndex());
    }
  }
break;
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return name;
}
