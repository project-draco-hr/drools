{
  try {
{
      pushFollow(FOLLOW_constraint_in_constraints2420);
      constraint(pattern);
      _fsp--;
      if (failed)       return;
      loop34:       do {
        int alt34=2;
        int LA34_0=input.LA(1);
        if ((LA34_0 == COMMA)) {
          alt34=1;
        }
switch (alt34) {
case 1:
{
            match(input,COMMA,FOLLOW_COMMA_in_constraints2427);
            if (failed)             return;
            pushFollow(FOLLOW_constraint_in_constraints2429);
            constraint(pattern);
            _fsp--;
            if (failed)             return;
          }
        break;
default :
      break loop34;
  }
}
 while (true);
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
