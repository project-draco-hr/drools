{
  if (input.LA(2) == ttype) {
    reportError(e);
    beginResync();
    input.consume();
    endResync();
    input.consume();
    return;
  }
  if (!recoverFromMismatchedElement(input,e,follow)) {
    throw e;
  }
}
