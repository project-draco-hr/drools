{
  Token t=null;
  OrDescr or=new OrDescr();
  try {
{
      pushFollow(FOLLOW_and_constr_in_or_constr2543);
      and_constr(or);
      _fsp--;
      if (failed)       return;
      loop39:       do {
        int alt39=2;
        int LA39_0=input.LA(1);
        if ((LA39_0 == DOUBLE_PIPE)) {
          alt39=1;
        }
switch (alt39) {
case 1:
{
            t=(Token)input.LT(1);
            match(input,DOUBLE_PIPE,FOLLOW_DOUBLE_PIPE_in_or_constr2553);
            if (failed)             return;
            if (backtracking == 0) {
              location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_START);
            }
            pushFollow(FOLLOW_and_constr_in_or_constr2563);
            and_constr(or);
            _fsp--;
            if (failed)             return;
          }
        break;
default :
      break loop39;
  }
}
 while (true);
if (backtracking == 0) {
  if (or.getDescrs().size() == 1) {
    base.addOrMerge((BaseDescr)or.getDescrs().get(0));
  }
 else   if (or.getDescrs().size() > 1) {
    base.addDescr(or);
  }
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
