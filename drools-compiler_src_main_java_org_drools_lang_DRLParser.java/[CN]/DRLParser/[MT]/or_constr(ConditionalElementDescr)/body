{
  OrDescr or=new OrDescr();
  try {
{
      pushFollow(FOLLOW_and_constr_in_or_constr3929);
      and_constr(or);
      _fsp--;
      if (failed)       return;
      loop70:       do {
        int alt70=2;
        int LA70_0=input.LA(1);
        if ((LA70_0 == DOUBLE_PIPE)) {
          alt70=1;
        }
switch (alt70) {
case 1:
{
            match(input,DOUBLE_PIPE,FOLLOW_DOUBLE_PIPE_in_or_constr3937);
            if (failed)             return;
            if (backtracking == 0) {
              location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_START);
            }
            pushFollow(FOLLOW_and_constr_in_or_constr3946);
            and_constr(or);
            _fsp--;
            if (failed)             return;
          }
        break;
default :
      break loop70;
  }
}
 while (true);
if (backtracking == 0) {
  if (or.getDescrs().size() == 1) {
    base.addOrMerge((BaseDescr)or.getDescrs().get(0));
  }
 else   if (or.getDescrs().size() > 1) {
    base.addDescr(or);
  }
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
