{
  OrDescr or=new OrDescr();
  try {
{
      pushFollow(FOLLOW_and_constr_in_or_constr3412);
      and_constr(or);
      _fsp--;
      if (failed)       return;
      loop63:       do {
        int alt63=2;
        int LA63_0=input.LA(1);
        if ((LA63_0 == DOUBLE_PIPE)) {
          alt63=1;
        }
switch (alt63) {
case 1:
{
            match(input,DOUBLE_PIPE,FOLLOW_DOUBLE_PIPE_in_or_constr3420);
            if (failed)             return;
            if (backtracking == 0) {
              location.setType(Location.LOCATION_LHS_INSIDE_CONDITION_START);
            }
            pushFollow(FOLLOW_and_constr_in_or_constr3429);
            and_constr(or);
            _fsp--;
            if (failed)             return;
          }
        break;
default :
      break loop63;
  }
}
 while (true);
if (backtracking == 0) {
  if (or.getDescrs().size() == 1) {
    base.addOrMerge((BaseDescr)or.getDescrs().get(0));
  }
 else   if (or.getDescrs().size() > 1) {
    base.addDescr(or);
  }
}
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
