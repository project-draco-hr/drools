{
  String text=null;
  Token cls=null;
  Token en=null;
  text=null;
  try {
{
{
        cls=(Token)input.LT(1);
        match(input,ID,FOLLOW_ID_in_enum_constraint2558);
        if (failed)         return text;
        match(input,33,FOLLOW_33_in_enum_constraint2560);
        if (failed)         return text;
        en=(Token)input.LT(1);
        match(input,ID,FOLLOW_ID_in_enum_constraint2564);
        if (failed)         return text;
      }
      if (backtracking == 0) {
        text=cls.getText() + "." + en.getText();
      }
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return text;
}
