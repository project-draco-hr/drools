{
  Token label=match(input,DRLLexer.ID,null,null,edType);
  if (state.failed)   return null;
  match(input,DRLLexer.UNIFY,null,null,DroolsEditorType.SYMBOL);
  if (state.failed)   return null;
  return label.getText();
}
