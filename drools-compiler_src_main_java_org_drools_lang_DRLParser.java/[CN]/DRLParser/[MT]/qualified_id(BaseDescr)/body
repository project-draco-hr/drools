{
  String name=null;
  Token loc=null;
  Token ID53=null;
  identifier_return ident=null;
  name=null;
  try {
{
      ID53=(Token)input.LT(1);
      match(input,ID,FOLLOW_ID_in_qualified_id4550);
      if (failed)       return name;
      if (backtracking == 0) {
        name=ID53.getText();
        if (descr != null) {
          descr.setStartCharacter(((CommonToken)ID53).getStartIndex());
          descr.setEndCharacter(((CommonToken)ID53).getStopIndex());
        }
      }
      loop75:       do {
        int alt75=2;
        int LA75_0=input.LA(1);
        if ((LA75_0 == DOT)) {
          alt75=1;
        }
switch (alt75) {
case 1:
{
            match(input,DOT,FOLLOW_DOT_in_qualified_id4561);
            if (failed)             return name;
            pushFollow(FOLLOW_identifier_in_qualified_id4565);
            ident=identifier();
            _fsp--;
            if (failed)             return name;
            if (backtracking == 0) {
              name+="." + ident.start.getText();
              if (descr != null) {
                descr.setEndCharacter(((CommonToken)((Token)ident.start)).getStopIndex());
              }
            }
          }
        break;
default :
      break loop75;
  }
}
 while (true);
loop76: do {
  int alt76=2;
  int LA76_0=input.LA(1);
  if ((LA76_0 == LEFT_SQUARE)) {
    alt76=1;
  }
switch (alt76) {
case 1:
{
      match(input,LEFT_SQUARE,FOLLOW_LEFT_SQUARE_in_qualified_id4587);
      if (failed)       return name;
      loc=(Token)input.LT(1);
      match(input,RIGHT_SQUARE,FOLLOW_RIGHT_SQUARE_in_qualified_id4591);
      if (failed)       return name;
      if (backtracking == 0) {
        name+="[]";
        if (descr != null) {
          descr.setEndCharacter(((CommonToken)loc).getStopIndex());
        }
      }
    }
  break;
default :
break loop76;
}
}
 while (true);
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return name;
}
