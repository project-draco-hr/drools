{
  BaseDescr d=null;
  Token loc=null;
  String c=null;
  d=null;
  try {
{
      loc=(Token)input.LT(1);
      match(input,72,FOLLOW_72_in_lhs_eval3045);
      if (failed)       return d;
      pushFollow(FOLLOW_paren_chunk_in_lhs_eval3049);
      c=paren_chunk();
      _fsp--;
      if (failed)       return d;
      if (backtracking == 0) {
        String body=c.substring(1,c.length() - 1);
        checkTrailingSemicolon(body,offset(loc.getLine()));
        d=new EvalDescr(body);
      }
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return d;
}
