{
  BaseDescr d=null;
  Token loc=null;
  String c=null;
  d=new EvalDescr();
  try {
{
      loc=(Token)input.LT(1);
      match(input,EVAL,FOLLOW_EVAL_in_lhs_eval3791);
      if (failed)       return d;
      pushFollow(FOLLOW_paren_chunk_in_lhs_eval3795);
      c=paren_chunk(d);
      _fsp--;
      if (failed)       return d;
      if (backtracking == 0) {
        if (loc != null)         d.setStartCharacter(((CommonToken)loc).getStartIndex());
        if (c != null) {
          String body=c.substring(1,c.length() - 1);
          checkTrailingSemicolon(body,offset(loc.getLine()));
          ((EvalDescr)d).setText(body);
        }
      }
    }
  }
 catch (  RecognitionException re) {
    reportError(re);
    recover(input,re);
  }
 finally {
  }
  return d;
}
