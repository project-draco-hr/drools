{
  this.key2.set(ClassWriter.NAME_TYPE,name,desc,null);
  Item result=get(this.key2);
  if (result == null) {
    put122(ClassWriter.NAME_TYPE,newUTF8(name),newUTF8(desc));
    result=new Item(this.index++,this.key2);
    put(result);
  }
  return result.index;
}
