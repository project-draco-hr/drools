{
  this.key2.set(ClassWriter.CLASS,value,null,null);
  Item result=get(this.key2);
  if (result == null) {
    this.pool.put12(ClassWriter.CLASS,newUTF8(value));
    result=new Item(this.index++,this.key2);
    put(result);
  }
  return result;
}
