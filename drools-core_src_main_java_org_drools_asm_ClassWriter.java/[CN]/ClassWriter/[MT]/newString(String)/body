{
  this.key2.set(ClassWriter.STR,value,null,null);
  Item result=get(this.key2);
  if (result == null) {
    this.pool.put12(ClassWriter.STR,newUTF8(value));
    result=new Item(this.index++,this.key2);
    put(result);
  }
  return result;
}
