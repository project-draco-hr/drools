{
  this.key3.set(ClassWriter.FIELD,owner,name,desc);
  Item result=get(this.key3);
  if (result == null) {
    put122(ClassWriter.FIELD,newClass(owner),newNameType(name,desc));
    result=new Item(this.index++,this.key3);
    put(result);
  }
  return result.index;
}
