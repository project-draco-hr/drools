{
  Object value=null;
  try {
    System.out.println(object);
    value=Introspector.getBeanInfo(this.clazz).getPropertyDescriptors()[this.index].getReadMethod().invoke(object,null);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return value;
}
