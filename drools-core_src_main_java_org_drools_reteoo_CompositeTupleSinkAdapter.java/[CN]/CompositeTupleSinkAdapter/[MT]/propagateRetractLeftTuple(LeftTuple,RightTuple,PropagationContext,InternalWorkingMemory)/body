{
  for (LeftTupleSinkNode sink=this.sinks.getFirst(); sink != null; sink=sink.getNextLeftTupleSinkNode()) {
    sink.retractLeftTuple(new LeftTuple(tuple,rightTuple,sink),context,workingMemory);
  }
}
