{
  ExecutionContext context=new ExecutionContext((InternalWorkingMemory)workingMemory,(ReteTuple)tuple,object,this.index - 1);
  return FieldFactory.getFieldValue(this.function.getBooleanValue(context));
}
