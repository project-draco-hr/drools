{
  try {
    return evaluator.evaluate(this.fieldExtractor.getValue(handle.getObject()),this.returnValueExpression.evaluate(tuple,this.requiredDeclarations,workingMemory));
  }
 catch (  Exception e) {
    throw new RuntimeDroolsException(e);
  }
}
