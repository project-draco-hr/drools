{
  final Iterator<WorkingMemoryEventListener> iter=getEventListenersIterator();
  if (iter.hasNext()) {
    final ObjectInsertedEvent event=new ObjectInsertedEvent(workingMemory,propagationContext,handle,object);
    do {
      iter.next().objectInserted(event);
    }
 while (iter.hasNext());
  }
}
