{
  String s=(String)get(name);
  if (s == null) {
    int p=name.indexOf('.');
    if (p != -1) {
      int q=name.indexOf('(');
      if (q != -1) {
        s=name.substring(p + 1,q);
      }
 else {
        s=name.substring(p + 1);
      }
    }
 else {
      s=name;
    }
  }
 else {
    unused.remove(name);
  }
  return s;
}
