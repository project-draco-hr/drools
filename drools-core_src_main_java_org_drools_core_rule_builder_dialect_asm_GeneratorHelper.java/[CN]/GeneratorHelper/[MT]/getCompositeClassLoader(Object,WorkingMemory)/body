{
  CompositeClassLoader classLoader=getCompositeClassLoader(obj);
  if (classLoader == null) {
    classLoader=((AbstractRuleBase)workingMemory.getRuleBase()).getRootClassLoader();
  }
  return classLoader;
}
