{
  if (!CNV.containsKey(in.getClass()))   throw new ConversionException("cannot convert type: " + in.getClass().getName() + " to: "+ primitiveType.getName());
  return CNV.get(in.getClass()).convert(in);
}
