{
  try {
    this.lock.lock();
    Activation[] queue=(Activation[])this.elements;
    this.elements=new Queueable[this.elements.length];
    this.size=0;
    return queue;
  }
  finally {
    this.lock.unlock();
  }
}
