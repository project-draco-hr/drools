{
  try {
    this.lock.lock();
    if (isEmpty()) {
      return null;
    }
    final Queueable result=this.elements[1];
    result.dequeue();
    return result;
  }
  finally {
    this.lock.unlock();
  }
}
