{
  final Object value=extractor2.getValue(workingMemory,handle2.getObject());
  final Collection col=(Collection)extractor1.getValue(workingMemory,handle1.getObject());
  return (col == null) ? true : !col.contains(value);
}
