{
  final Object array=extractor1.getValue(workingMemory,handle1.getObject());
  if (array == null)   return false;
  return !getArrayContains(array.getClass()).contains(array,workingMemory,extractor2,handle2.getObject());
}
