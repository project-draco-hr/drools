{
  KnowledgeBuilderConfiguration kconf=(KnowledgeBuilderConfiguration)context.getContextManager().getDefaultContext().get(kbuilderConfId);
  kconf.setProperty(this.name,this.value);
  return null;
}
