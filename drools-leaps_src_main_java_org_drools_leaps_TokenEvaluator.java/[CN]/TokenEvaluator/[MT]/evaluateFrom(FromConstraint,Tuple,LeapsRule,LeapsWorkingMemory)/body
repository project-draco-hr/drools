{
  for (final Iterator it=from.getProvider().getResults(tuple,workingMemory,null); it.hasNext(); ) {
    final Object object=it.next();
    if (from.isAllowed(new DefaultFactHandle(-1,object),tuple,workingMemory)) {
      return object;
    }
  }
  return null;
}
