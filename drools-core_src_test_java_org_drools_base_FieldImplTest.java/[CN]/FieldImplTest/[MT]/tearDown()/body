{
  super.tearDown();
  Assert.assertEquals(field1,field1);
  Assert.assertEquals(field1,field2);
  Assert.assertEquals(field3,field3);
  Assert.assertEquals(field3,field4);
  Assert.assertFalse(field1.equals(field3));
  Assert.assertFalse(field3.equals(field1));
  Assert.assertFalse(field3.equals(field5));
}
