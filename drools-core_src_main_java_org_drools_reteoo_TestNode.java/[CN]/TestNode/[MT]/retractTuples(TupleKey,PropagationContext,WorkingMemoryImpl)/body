{
  if (hasMemory()) {
    Map memory=(Map)workingMemory.getNodeMemory(this);
    if (memory.remove(key) != null) {
      propagateRetractTuples(key,context,workingMemory);
    }
  }
 else {
    propagateRetractTuples(key,context,workingMemory);
  }
}
