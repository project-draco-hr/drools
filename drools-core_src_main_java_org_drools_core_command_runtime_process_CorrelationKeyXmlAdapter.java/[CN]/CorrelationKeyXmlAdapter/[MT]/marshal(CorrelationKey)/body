{
  String result=null;
  for (  CorrelationProperty<?> p : key.getProperties()) {
    if (result == null) {
      result=(String)p.getValue();
    }
 else {
      result+="," + p.getValue();
    }
  }
  return result;
}
