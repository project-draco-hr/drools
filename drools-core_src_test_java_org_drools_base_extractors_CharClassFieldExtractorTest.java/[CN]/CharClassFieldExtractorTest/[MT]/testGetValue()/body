{
  try {
    Assert.assertEquals(new Character('a'),this.extractor.getValue(this.bean));
    Assert.assertTrue(this.extractor.getValue(this.bean) instanceof Character);
  }
 catch (  final Exception e) {
    fail("Should not throw an exception");
  }
}
