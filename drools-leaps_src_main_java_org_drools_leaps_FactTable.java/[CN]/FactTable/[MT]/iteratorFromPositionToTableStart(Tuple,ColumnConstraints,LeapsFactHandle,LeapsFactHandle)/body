{
  TableIterator ret=((HashedTableComponent)this.notAndExistsHashedTables.get(constraint)).iteratorFromPositionToTableStart(tuple,startFactHandle,currentFactHandle);
  if (ret == null) {
    ret=Table.emptyIterator();
  }
  return ret;
}
