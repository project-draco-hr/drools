{
  final Entry entry=getEntry(key,keyHash);
  if (entry == null) {
    addEntry(keyHash,key,value);
    return null;
  }
  final Object prevValue=entry._value;
  entry._value=value;
  return prevValue;
}
