{
  if (this._isShared) {
    return removeShared(key);
  }
  final Entry entry=getEntry(key);
  if (entry != null) {
    final Object prevValue=entry._value;
    removeEntry(entry);
    return prevValue;
  }
  return null;
}
