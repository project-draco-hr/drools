{
  BaseClassFieldWriter reader=this.writeCache.get(key);
  if (reader == null) {
    reader=ClassFieldAccessorFactory.getInstance().getClassFieldWriter(cls,key.getFieldName(),this);
    this.writeCache.put(key,reader);
  }
  return reader;
}
