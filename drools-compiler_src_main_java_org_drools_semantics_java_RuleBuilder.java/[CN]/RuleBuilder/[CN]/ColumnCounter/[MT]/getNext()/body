{
  if (this.ge != null && this.ge.getClass() == Or.class) {
    if (!this.orCheck) {
      this.orCheck=true;
      return ++this.value;
    }
 else {
      return this.value;
    }
  }
 else {
    return ++this.value;
  }
}
