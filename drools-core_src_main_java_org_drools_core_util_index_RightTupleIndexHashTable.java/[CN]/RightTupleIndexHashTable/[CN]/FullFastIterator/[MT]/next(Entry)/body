{
  Tuple rightTuple=(Tuple)object;
  TupleList list=null;
  if (rightTuple != null) {
    list=(TupleList)rightTuple.getMemory();
  }
  int length=table.length;
  while (this.row <= length) {
    while (list == null) {
      if (this.row < length) {
        list=(TupleList)this.table[this.row];
        this.row++;
      }
 else {
        return null;
      }
      if (list != null) {
        rightTuple=(Tuple)list.getFirst();
        return rightTuple;
      }
    }
    rightTuple=(Tuple)rightTuple.getNext();
    if (rightTuple != null) {
      return rightTuple;
    }
 else {
      list=(TupleList)list.getNext();
      if (list != null) {
        rightTuple=(Tuple)list.getFirst();
        return rightTuple;
      }
    }
  }
  return null;
}
