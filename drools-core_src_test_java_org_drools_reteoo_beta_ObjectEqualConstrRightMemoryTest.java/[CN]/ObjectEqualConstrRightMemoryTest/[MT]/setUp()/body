{
  super.setUp();
  ClassFieldExtractor extractor=new ClassFieldExtractor(DummyValueObject.class,"objectAttr");
  Declaration declaration=new Declaration("myObject",extractor,0);
  Evaluator evaluator=EvaluatorFactory.getEvaluator(Evaluator.OBJECT_TYPE,Evaluator.EQUAL);
  this.memory=new ObjectEqualConstrRightMemory(extractor,declaration,evaluator,this.child);
}
