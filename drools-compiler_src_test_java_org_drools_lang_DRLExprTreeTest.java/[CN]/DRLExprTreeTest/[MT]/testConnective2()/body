{
  String source="(a > b || 10 != 20) && someMethod(10) == 20 @";
  Object result=parse("conditionalExpression","expression",source);
  assertFalse(parser.getErrorMessages().toString(),parser.hasErrors());
  System.out.println(result);
}
