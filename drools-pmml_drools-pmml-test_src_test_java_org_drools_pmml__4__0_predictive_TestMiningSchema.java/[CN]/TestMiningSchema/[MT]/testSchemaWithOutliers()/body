{
  setKSession(getModelSession(source,VERBOSE));
  setKbase(getKSession().getKnowledgeBase());
  getKSession().getWorkingMemoryEntryPoint("in_PetalLength").insert(0.24);
  getKSession().fireAllRules();
  checkFirstDataFieldOfTypeStatus(getKbase().getFactType(packageName,"PetalLength"),true,false,"IRIS_MLP",1.0);
  refreshKSession();
  getKSession().getWorkingMemoryEntryPoint("in_PetalLength").insert(999.9);
  getKSession().fireAllRules();
  checkFirstDataFieldOfTypeStatus(getKbase().getFactType(packageName,"PetalLength"),true,false,"IRIS_MLP",6.9);
}
