{
  final TestInterface obj=new TestInterfaceImpl();
  final ClassFieldExtractor ext=cache.getExtractor(TestInterface.class,"something",getClass().getClassLoader());
  assertEquals("foo",(String)ext.getValue(null,obj));
}
