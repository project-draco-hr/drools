{
  final ConcreteChild obj=new ConcreteChild();
  final ClassFieldExtractor ext=cache.getExtractor(InterfaceChild.class,"foo",getClass().getClassLoader());
  assertEquals(42,((Number)ext.getValue(null,obj)).intValue());
}
