{
  final ConcreteChild obj=new ConcreteChild();
  final ClassFieldExtractor ext=ClassFieldExtractorCache.getExtractor(InterfaceChild.class,"foo",getClass().getClassLoader());
  assertEquals(42,((Number)ext.getValue(obj)).intValue());
}
