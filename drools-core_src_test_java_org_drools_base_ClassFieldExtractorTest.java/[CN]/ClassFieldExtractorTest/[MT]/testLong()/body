{
  final ClassFieldExtractor ext=ClassFieldExtractorCache.getExtractor(TestBean.class,"longField",getClass().getClassLoader());
  final TestBean bean=new TestBean();
  assertEquals(424242,((Number)ext.getValue(bean)).longValue());
}
