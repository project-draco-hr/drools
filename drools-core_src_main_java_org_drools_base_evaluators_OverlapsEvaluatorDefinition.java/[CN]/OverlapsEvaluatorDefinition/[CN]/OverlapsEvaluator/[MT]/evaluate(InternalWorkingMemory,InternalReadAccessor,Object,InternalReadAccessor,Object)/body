{
  if (extractor1.isNullValue(workingMemory,object1)) {
    return false;
  }
  long startTS=((EventFactHandle)object2).getStartTimestamp();
  long endTS=((EventFactHandle)object1).getEndTimestamp();
  long dist=endTS - startTS;
  return this.getOperator().isNegated() ^ (((EventFactHandle)object1).getStartTimestamp() < startTS && endTS < ((EventFactHandle)object2).getEndTimestamp() && dist >= this.minDev && dist <= this.maxDev);
}
