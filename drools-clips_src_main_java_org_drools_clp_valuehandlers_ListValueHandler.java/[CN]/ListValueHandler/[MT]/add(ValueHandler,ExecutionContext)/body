{
  ValueHandler nested=valueHandler.getValue(context);
  valueHandler=(nested != null) ? nested : valueHandler;
  if (valueHandler instanceof ListValueHandler) {
    add((ListValueHandler)valueHandler,context);
  }
 else {
    add(valueHandler);
  }
}
