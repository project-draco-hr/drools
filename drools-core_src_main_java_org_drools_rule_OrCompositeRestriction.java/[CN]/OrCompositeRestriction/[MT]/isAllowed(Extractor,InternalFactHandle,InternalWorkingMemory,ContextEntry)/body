{
  for (int i=0, ilength=this.restrictions.length; i < ilength; i++) {
    if (this.restrictions[i].isAllowed(extractor,handle,workingMemory,context)) {
      return true;
    }
  }
  return false;
}
