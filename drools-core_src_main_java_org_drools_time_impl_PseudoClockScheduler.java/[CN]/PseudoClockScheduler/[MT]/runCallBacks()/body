{
  ScheduledJob item=queue.peek();
  while (item != null && item.getTrigger().getNextFireTime().getTime() <= this.timer) {
    queue.remove();
    try {
      item.call();
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
    item=queue.peek();
  }
}
