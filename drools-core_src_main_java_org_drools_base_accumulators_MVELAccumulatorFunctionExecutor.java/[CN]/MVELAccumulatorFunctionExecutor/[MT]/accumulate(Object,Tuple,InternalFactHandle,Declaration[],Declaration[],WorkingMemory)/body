{
  this.factory.setContext(leftTuple,null,handle.getObject(),workingMemory);
  final Object value=MVEL.executeExpression(this.expression,this.dummy,this.factory);
  this.function.accumulate(context,value);
}
