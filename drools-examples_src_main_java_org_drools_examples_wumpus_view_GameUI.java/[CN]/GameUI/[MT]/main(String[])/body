{
  EventQueue.invokeLater(new Runnable(){
    public void run(){
      try {
        GameUI window=new GameUI();
        window.initialize();
      }
 catch (      Exception e) {
        e.printStackTrace();
      }
    }
  }
);
}
