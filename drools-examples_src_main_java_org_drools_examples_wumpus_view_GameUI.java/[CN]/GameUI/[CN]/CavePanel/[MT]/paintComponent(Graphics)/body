{
  if (bi == null) {
    bi=new BufferedImage(getWidth(),getHeight(),BufferedImage.TYPE_INT_RGB);
    caveG=bi.createGraphics();
    caveG.setColor(Color.WHITE);
    caveG.fillRect(0,0,getWidth(),getHeight());
  }
  g.drawImage(bi,0,0,null);
}
