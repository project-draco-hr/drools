{
  try {
    int _type=FROM;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("from");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
