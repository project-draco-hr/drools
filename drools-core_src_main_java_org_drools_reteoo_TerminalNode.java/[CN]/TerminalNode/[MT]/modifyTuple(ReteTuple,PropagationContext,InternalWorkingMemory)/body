{
  boolean fireActivation=true;
  Activation activation=tuple.getActivation();
  if (activation.isActivated()) {
    activation.remove();
    fireActivation=false;
  }
  assertTuple(tuple,context,workingMemory,fireActivation);
}
