{
  Activation activation=tuple.getActivation();
  if (activation != null) {
    activation.remove();
    workingMemory.getAgendaEventSupport().fireActivationCancelled(activation);
  }
  workingMemory.removeLogicalAssertions(tuple,context,this.rule);
}
