{
  final TestInterface obj=new TestInterfaceImpl();
  final ClassFieldReader ext=cache.getReader(TestInterface.class,"something",getClass().getClassLoader());
  assertEquals("foo",(String)ext.getValue(null,obj));
}
