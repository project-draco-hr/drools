{
  PatternDescr d;
  PatternDescr left=null;
  PatternDescr right=null;
  d=null;
  try {
{
      OrDescr or=null;
      following.push(FOLLOW_lhs_and_in_lhs_or1395);
      left=lhs_and();
      following.pop();
      d=left;
      loop34:       do {
        int alt34=2;
        int LA34_0=input.LA(1);
        if ((LA34_0 >= 36 && LA34_0 <= 37)) {
          int LA34_11=input.LA(2);
          if (!(expander != null)) {
            alt34=1;
          }
        }
switch (alt34) {
case 1:
{
            if ((input.LA(1) >= 36 && input.LA(1) <= 37)) {
              input.consume();
              errorRecovery=false;
            }
 else {
              MismatchedSetException mse=new MismatchedSetException(null,input);
              recoverFromMismatchedSet(input,mse,FOLLOW_set_in_lhs_or1405);
              throw mse;
            }
            following.push(FOLLOW_lhs_and_in_lhs_or1416);
            right=lhs_and();
            following.pop();
            if (or == null) {
              or=new OrDescr();
              or.addDescr(left);
              d=or;
            }
            or.addDescr(right);
          }
        break;
default :
      break loop34;
  }
}
 while (true);
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return d;
}
