{
  PatternDescr d;
  PatternDescr left=null;
  PatternDescr right=null;
  d=null;
  try {
{
      OrDescr or=null;
      following.push(FOLLOW_lhs_and_in_lhs_or2547);
      left=lhs_and();
      following.pop();
      d=left;
      loop60:       do {
        int alt60=2;
        int LA60_0=input.LA(1);
        if ((LA60_0 >= 41 && LA60_0 <= 42)) {
          alt60=1;
        }
switch (alt60) {
case 1:
{
            if ((input.LA(1) >= 41 && input.LA(1) <= 42)) {
              input.consume();
              errorRecovery=false;
            }
 else {
              MismatchedSetException mse=new MismatchedSetException(null,input);
              recoverFromMismatchedSet(input,mse,FOLLOW_set_in_lhs_or2556);
              throw mse;
            }
            following.push(FOLLOW_opt_eol_in_lhs_or2561);
            opt_eol();
            following.pop();
            following.push(FOLLOW_lhs_and_in_lhs_or2568);
            right=lhs_and();
            following.pop();
            if (or == null) {
              or=new OrDescr();
              or.addDescr(left);
              d=or;
            }
            or.addDescr(right);
          }
        break;
default :
      break loop60;
  }
}
 while (true);
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return d;
}
