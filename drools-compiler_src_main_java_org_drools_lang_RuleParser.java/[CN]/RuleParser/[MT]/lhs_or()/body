{
  PatternDescr d;
  PatternDescr left=null;
  PatternDescr right=null;
  d=null;
  try {
{
      OrDescr or=null;
      following.push(FOLLOW_lhs_and_in_lhs_or2439);
      left=lhs_and();
      following.pop();
      d=left;
      loop58:       do {
        int alt58=2;
        int LA58_0=input.LA(1);
        if ((LA58_0 >= 41 && LA58_0 <= 42)) {
          alt58=1;
        }
switch (alt58) {
case 1:
{
            if ((input.LA(1) >= 41 && input.LA(1) <= 42)) {
              input.consume();
              errorRecovery=false;
            }
 else {
              MismatchedSetException mse=new MismatchedSetException(null,input);
              recoverFromMismatchedSet(input,mse,FOLLOW_set_in_lhs_or2448);
              throw mse;
            }
            following.push(FOLLOW_opt_eol_in_lhs_or2453);
            opt_eol();
            following.pop();
            following.push(FOLLOW_lhs_and_in_lhs_or2460);
            right=lhs_and();
            following.pop();
            if (or == null) {
              or=new OrDescr();
              or.addDescr(left);
              d=or;
            }
            or.addDescr(right);
          }
        break;
default :
      break loop58;
  }
}
 while (true);
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return d;
}
