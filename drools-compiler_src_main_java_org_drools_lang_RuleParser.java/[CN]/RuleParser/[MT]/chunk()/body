{
  String text;
  Token any=null;
  String c=null;
  text=null;
  try {
{
      loop34:       do {
        int alt34=3;
        alt34=dfa34.predict(input);
switch (alt34) {
case 1:
{
{
              any=(Token)input.LT(1);
              matchAny(input);
              if (text == null) {
                text=any.getText();
              }
 else {
                text=text + " " + any.getText();
              }
            }
          }
        break;
case 2:
{
{
          match(input,28,FOLLOW_28_in_chunk1353);
          following.push(FOLLOW_chunk_in_chunk1357);
          c=chunk();
          following.pop();
          match(input,29,FOLLOW_29_in_chunk1359);
          if (text == null) {
            text="( " + c + " )";
          }
 else {
            text=text + " ( " + c+ " )";
          }
        }
      }
    break;
default :
  break loop34;
}
}
 while (true);
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return text;
}
