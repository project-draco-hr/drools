{
  Token fb=null;
  Token f=null;
  Token op=null;
  Token bvc=null;
  String lc=null;
  String rvc=null;
  PatternDescr d=null;
  try {
{
      following.push(FOLLOW_opt_eol_in_constraint1445);
      opt_eol();
      following.pop();
      int alt42=2;
      alt42=dfa42.predict(input);
switch (alt42) {
case 1:
{
          fb=(Token)input.LT(1);
          match(input,ID,FOLLOW_ID_in_constraint1453);
          following.push(FOLLOW_opt_eol_in_constraint1455);
          opt_eol();
          following.pop();
          match(input,29,FOLLOW_29_in_constraint1457);
          following.push(FOLLOW_opt_eol_in_constraint1459);
          opt_eol();
          following.pop();
        }
      break;
  }
  f=(Token)input.LT(1);
  match(input,ID,FOLLOW_ID_in_constraint1469);
  if (fb != null) {
    System.err.println("fb: " + fb.getText());
    System.err.println(" f: " + f.getText());
    d=new FieldBindingDescr(f.getText(),fb.getText());
    System.err.println("fbd: " + d);
    d.setLocation(f.getLine(),f.getCharPositionInLine());
    constraints.add(d);
  }
  following.push(FOLLOW_opt_eol_in_constraint1479);
  opt_eol();
  following.pop();
  int alt44=2;
  int LA44_0=input.LA(1);
  if (LA44_0 == 37 || (LA44_0 >= 39 && LA44_0 <= 45)) {
    alt44=1;
  }
 else   if (LA44_0 == EOL || (LA44_0 >= 21 && LA44_0 <= 22)) {
    alt44=2;
  }
 else {
    NoViableAltException nvae=new NoViableAltException("459:33: (op= (\'==\'|\'>\'|\'>=\'|\'<\'|\'<=\'|\'!=\'|\'contains\'|\'matches\') opt_eol (bvc= ID | lc= literal_constraint | rvc= retval_constraint ) )?",44,0,input);
    throw nvae;
  }
switch (alt44) {
case 1:
{
      op=(Token)input.LT(1);
      if (input.LA(1) == 37 || (input.LA(1) >= 39 && input.LA(1) <= 45)) {
        input.consume();
        errorRecovery=false;
      }
 else {
        MismatchedSetException mse=new MismatchedSetException(null,input);
        recoverFromMismatchedSet(input,mse,FOLLOW_set_in_constraint1487);
        throw mse;
      }
      following.push(FOLLOW_opt_eol_in_constraint1559);
      opt_eol();
      following.pop();
      int alt43=3;
switch (input.LA(1)) {
case ID:
        alt43=1;
      break;
case INT:
case STRING:
case FLOAT:
    alt43=2;
  break;
case 20:
alt43=3;
break;
default :
NoViableAltException nvae=new NoViableAltException("469:41: (bvc= ID | lc= literal_constraint | rvc= retval_constraint )",43,0,input);
throw nvae;
}
switch (alt43) {
case 1:
{
bvc=(Token)input.LT(1);
match(input,ID,FOLLOW_ID_in_constraint1577);
d=new BoundVariableDescr(f.getText(),op.getText(),bvc.getText());
d.setLocation(f.getLine(),f.getCharPositionInLine());
constraints.add(d);
}
break;
case 2:
{
following.push(FOLLOW_literal_constraint_in_constraint1602);
lc=literal_constraint();
following.pop();
d=new LiteralDescr(f.getText(),op.getText(),lc);
d.setLocation(f.getLine(),f.getCharPositionInLine());
constraints.add(d);
}
break;
case 3:
{
following.push(FOLLOW_retval_constraint_in_constraint1622);
rvc=retval_constraint();
following.pop();
d=new ReturnValueDescr(f.getText(),op.getText(),rvc);
d.setLocation(f.getLine(),f.getCharPositionInLine());
constraints.add(d);
}
break;
}
}
break;
}
following.push(FOLLOW_opt_eol_in_constraint1655);
opt_eol();
following.pop();
}
}
 catch (RecognitionException re) {
reportError(re);
recover(input,re);
}
 finally {
}
return;
}
