{
  InternalKieJar oldKieJar=kieJars.remove(getKieJarKey(kieJar));
  if (oldKieJar != null) {
    for (    String kieBaseName : oldKieJar.getKieBaseNames()) {
      removeKieBase(kieBaseName);
    }
  }
  addKieJar(kieJar);
  kSessions=indexKieSessions();
}
