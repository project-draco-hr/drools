{
  Object expectedResult=new Object();
  controlCallback.expectAndReturn((mockCallback).doInWorkingMemory(mockWorkingMemory),expectedResult);
  mocks.replay();
  Object result=template.execute(mockCallback);
  mocks.verify();
  assertSame(expectedResult,result);
}
