{
  StatefulKnowledgeSessionPipelineContext pContext=(StatefulKnowledgeSessionPipelineContext)context;
  FactHandle handle=((pContext).getKsession()).insert(object);
  ((pContext).getHandles()).put(handle,object);
}
