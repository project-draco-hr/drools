{
  getWorkingMemory().addPropagation(new PropagationEntry.AbstractPropagationEntry(){
    @Override public void execute(    InternalWorkingMemory wm){
      timerInstances.remove(jobContext.getJobHandle().getId());
    }
  }
);
}
