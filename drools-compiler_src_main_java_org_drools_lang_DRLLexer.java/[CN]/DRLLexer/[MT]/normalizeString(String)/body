{
  if (input != null && input.length() >= 4) {
    input=input.substring(1,input.length() - 1);
    input.replaceAll("\'","'");
    input.replaceAll("\"","\\\"");
    input="\"" + input + "\"";
  }
  return input;
}
