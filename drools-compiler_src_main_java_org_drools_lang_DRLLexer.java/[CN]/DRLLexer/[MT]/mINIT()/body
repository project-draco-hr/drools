{
  try {
    int _type=INIT;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("init");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
