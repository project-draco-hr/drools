{
  try {
    int _type=END;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("end");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
