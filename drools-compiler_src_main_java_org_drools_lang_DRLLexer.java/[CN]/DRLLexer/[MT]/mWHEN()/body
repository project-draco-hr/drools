{
  try {
    int _type=WHEN;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("when");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
