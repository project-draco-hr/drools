{
  try {
    int _type=OVER;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("over");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
