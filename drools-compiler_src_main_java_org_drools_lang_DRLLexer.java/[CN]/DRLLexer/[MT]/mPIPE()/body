{
  try {
    int _type=PIPE;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match('|');
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
