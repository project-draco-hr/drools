{
  try {
    int _type=COLLECT;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      match("collect");
      if (state.failed)       return;
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}
