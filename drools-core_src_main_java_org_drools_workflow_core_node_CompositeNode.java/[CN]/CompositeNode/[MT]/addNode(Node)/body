{
  long id=0;
  for (  Node n : nodeContainer.getNodes()) {
    if (n.getId() > id) {
      id=n.getId();
    }
  }
  node.setId(++id);
  nodeContainer.addNode(node);
  node.setNodeContainer(this);
}
