{
  parseResource("eval_with_semicolon.drl").compilationUnit();
  assertTrue(this.parser.hasErrors());
  assertEquals(1,this.parser.getErrorMessages().size());
  assertEquals("ERR 102",this.parser.getErrors().get(0).getErrorCode());
}
