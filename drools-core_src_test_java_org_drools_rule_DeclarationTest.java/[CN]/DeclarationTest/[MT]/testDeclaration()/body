{
  FieldExtractor extractor=new ClassFieldExtractor(Cheese.class,getIndex(Cheese.class,"type"));
  Declaration declaration=new Declaration(3,"typeOfCheese",extractor,5);
  assertEquals(3,declaration.getIndex());
  assertEquals("typeOfCheese",declaration.getIdentifier());
  assertSame(String.class,declaration.getDeclarationType());
  assertSame(extractor,declaration.getExtractor());
  assertEquals(5,declaration.getColumn());
}
