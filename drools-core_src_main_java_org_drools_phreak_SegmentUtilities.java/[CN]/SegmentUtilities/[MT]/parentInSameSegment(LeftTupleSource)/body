{
  LeftTupleSource parent=lt.getLeftTupleSource();
  if (parent != null && (parent.getSinkPropagator().size() == 1 || (parent.getSinkPropagator().size() == 2 && NodeTypeEnums.isBetaNode(lt) && ((BetaNode)lt).isRightInputIsRiaNode()))) {
    return true;
  }
 else {
    return false;
  }
}
