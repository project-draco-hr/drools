{
  if (map.containsKey(key)) {
    Set<Redundancy> set=map.get(key);
    boolean exists=set.remove(value);
    if (set.isEmpty()) {
      map.remove(key);
    }
    return exists;
  }
  return false;
}
