{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Broken broken=(Broken)o;
  if (!bird.equals(broken.bird)) {
    return false;
  }
  if (!part.equals(broken.part)) {
    return false;
  }
  return true;
}
