{
  String descrString="";
  for (final Iterator it=descr.iterator(); it.hasNext(); ) {
    visit(it.next());
    descrString+=this.template;
    descrString+=DrlDumper.eol;
  }
  return descrString;
}
