{
  System.out.println("generate tree");
  RBTree<Integer,String> tree=new RBTree<Integer,String>();
  for (int i=0; i <= range; i=i + increment) {
    tree.insert(i,"" + i);
  }
  System.out.println("test data with tree");
  checkResults(tree,range,start,end,increment);
}
