{
  Object d=this.delay.getValue(item,((AgendaItem)item).getTerminalNode().getTimerDelayDeclarations(),item.getRule(),wm);
  if (d instanceof Number) {
    return ((Number)d).longValue();
  }
 else {
    return TimeUtils.parseTimeString(d.toString());
  }
}
