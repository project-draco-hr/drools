{
  final Thread caller=Thread.currentThread();
synchronized (this) {
    if (this.owner_ == null) {
      this.owner_=caller;
      this.holds_=1;
      return true;
    }
 else     if (caller == this.owner_) {
      incHolds();
      return true;
    }
  }
  return false;
}
