{
  if (this.alphaConstraints.length > 0) {
    for (int i=0; i < this.alphaConstraints.length; i++) {
      if (this.alphaConstraints[i].isAllowed(handle,workingMemory)) {
        return true;
      }
    }
    return false;
  }
  return true;
}
