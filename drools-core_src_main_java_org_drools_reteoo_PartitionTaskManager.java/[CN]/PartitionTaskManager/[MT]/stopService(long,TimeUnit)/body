{
  boolean result=true;
  if (isRunning()) {
    this.task.shutdown();
    this.pool.shutdown();
    try {
      result=this.pool.awaitTermination(timeout,unit);
    }
 catch (    InterruptedException e) {
      result=false;
    }
    this.pool=null;
  }
  return result;
}
