{
  if (in != null) {
    try {
      StockTick tick=load();
      next=new EventImpl<StockTick>(tick.getTimestamp(),tick);
      return true;
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
  return false;
}
