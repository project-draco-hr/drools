{
  try {
    if (object != null) {
      final Method method=getMethod(object.getClass());
      method.invoke(this,new Object[]{object});
    }
 else {
      final Method method=getClass().getMethod("visitNull",(Class[])null);
      method.invoke(this,(Object[])null);
    }
  }
 catch (  final Exception e) {
    e.printStackTrace();
  }
}
