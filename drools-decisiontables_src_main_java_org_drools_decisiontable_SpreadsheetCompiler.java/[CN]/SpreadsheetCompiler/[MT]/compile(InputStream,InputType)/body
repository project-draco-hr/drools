{
  final RuleSheetListener listener=getRuleSheetListener(xlsStream,type);
  final Package rulePackage=listener.getRuleSet();
  final DRLOutput out=new DRLOutput();
  rulePackage.renderDRL(out);
  return out.getDRL();
}
