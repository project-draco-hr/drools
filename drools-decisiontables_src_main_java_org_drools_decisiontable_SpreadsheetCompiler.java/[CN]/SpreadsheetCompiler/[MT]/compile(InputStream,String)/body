{
  RuleSheetListener listener=getRuleSheetListener(stream,worksheetName);
  Package rulePackage=listener.getRuleSet();
  DRLOutput out=new DRLOutput();
  rulePackage.renderDRL(out);
  return out.getDRL();
}
