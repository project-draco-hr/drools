{
  if (thing == core) {
    return thing;
  }
  ((TraitProxy)thing).setTypeFilter(boundary);
  if (logical) {
    insertLogical(thing,mode);
  }
 else {
    insert(thing);
  }
  ((TraitProxy)thing).setTypeFilter(null);
  return thing;
}
