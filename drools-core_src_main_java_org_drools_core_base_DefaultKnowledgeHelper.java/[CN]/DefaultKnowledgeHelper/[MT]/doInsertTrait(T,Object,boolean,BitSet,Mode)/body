{
  if (thing == core) {
    return thing;
  }
  ((TraitProxy)thing).setTypeFilter(boundary);
  if (logical) {
    insertLogical(thing,modes);
  }
 else {
    insert(thing);
  }
  ((TraitProxy)thing).setTypeFilter(null);
  return thing;
}
