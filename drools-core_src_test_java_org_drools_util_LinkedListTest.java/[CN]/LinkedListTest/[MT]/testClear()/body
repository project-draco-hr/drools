{
  list.add(node1);
  list.add(node2);
  list.add(node3);
  Assert.assertEquals("List size should be 3",list.size(),3);
  list.clear();
  Assert.assertEquals("Empty list should have size 0",list.size(),0);
}
