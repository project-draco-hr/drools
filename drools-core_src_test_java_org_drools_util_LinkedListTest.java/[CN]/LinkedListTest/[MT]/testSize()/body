{
  list.add(node1);
  Assert.assertEquals("LinkedList should have 1 node",list.size(),1);
  list.add(node2);
  Assert.assertEquals("LinkedList should have 2 nodes",list.size(),2);
  list.add(node3);
  Assert.assertEquals("LinkedList should have 3 nodes",list.size(),3);
}
