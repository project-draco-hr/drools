{
  this.ident-=2;
  try {
    if (this.openElement) {
      this.w.write("/>\n");
      this.openElement=false;
    }
 else {
      writeIdent();
      this.w.write("</" + qName + ">\n");
    }
  }
 catch (  final IOException ex) {
    throw new SAXException(ex);
  }
}
