{
switch (type) {
case RuleBase.RETEOO:
    return new org.drools.reteoo.ReteooRuleBase(UUID.randomUUID().toString(),config);
default :
  throw new IllegalArgumentException("Unknown engine type: " + type);
}
}
