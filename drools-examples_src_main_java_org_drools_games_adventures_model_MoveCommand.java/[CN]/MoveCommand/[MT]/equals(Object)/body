{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  MoveCommand that=(MoveCommand)o;
  if (!character.equals(that.character)) {
    return false;
  }
  if (!room.equals(that.room)) {
    return false;
  }
  return true;
}
