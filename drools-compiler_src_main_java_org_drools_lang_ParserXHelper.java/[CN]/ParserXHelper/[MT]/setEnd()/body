{
  Token last=input.LT(-1);
  if (last != null && !builderContext.empty()) {
    builderContext.peek().endCharacter(((CommonToken)last).getStopIndex() + 1).endLocation(last.getLine(),last.getCharPositionInLine() + last.getText().length() - 1);
  }
}
